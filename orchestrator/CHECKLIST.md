# Orchestrator Checklist

End-to-end progression for the Agent Avila dual-truth fix and surrounding orchestrator work.

## Completed phases

- [x] Phase 1 — Repo / Safety Baseline Audit (closed 2026-05-01)
- [x] Phase 1.5 — Deep Audit + Gap Closure
- [x] Phase 2 — Position Truth Audit (read-only) — surfaced P0-1 / P0-2 dual-truth risks
- [x] Phase O-1 — Blueprint (`orchestrator/BLUEPRINT.md`)
- [x] Phase O-2 — Project Attachment Audit (read-only)
- [x] Phase O-3 — 3-Brain CLI Verification (Claude builder, Codex reviewer)
- [x] Phase A.1 — Paper shadow helpers refactored to async + `{ ok, reason }` return contract — commit `5bcda59`
- [x] Phase A.2 — Paper BUY/CLOSE DB-first persistence; LOG_FILE best-effort post-DB; live unchanged — commit `959fef7`
- [x] Orchestrator BLUEPRINT and FIX-PLAN committed — commit `685a905`
- [x] Phase O-4 — Orchestrator automation layer (STATUS, CHECKLIST, APPROVAL-GATES, NEXT-ACTION, AUTOPILOT-RULES, prompts/) — commit `f080b24`
- [x] Phase B.1 — Paper close-source cleanup — commit `cb7facb` (closeout `63bbac4`)
  - [x] B.1 design approved by Codex (4 review rounds, final = APPROVE)
  - [x] B.1 implementation written to `dashboard.js` (3 hunks: helper hardening, CLOSE_POSITION rewrite, SELL_ALL rewrite)
  - [x] B.1 Codex implementation review = PASS-WITH-NOTES
  - [x] B.1 committed (`cb7facb`)
  - [x] B.1 closeout docs committed (`63bbac4`)
- [x] **Phase B.2a — Infrastructure: transactional helper + event_type CHECK extension — commit `a324290`** (closeout `f081b6f`)
  - [x] B.2a design audit found existing `updatePositionRiskLevels` already does similar work; chose Option β (additive transactional variant)
  - [x] B.2a design Codex-approved (3 review rounds: initial → Option β → final)
  - [x] B.2a pre-flight: live constraint name verified (`trade_events_event_type_check`) via safe non-secret-printing query
  - [x] B.2a implementation: new `updatePositionRiskLevelsTx(client, mode, orderId, fields)` in db.js (existing `updatePositionRiskLevels` untouched)
  - [x] B.2a implementation: new migration `007_event_type_sl_tp_updates.sql` (idempotent ALTER CHECK extending allowed event types from 8 to 10)
  - [x] `node --check db.js` PASS
  - [x] B.2a Codex implementation review = PASS-WITH-NOTES (no required edits)
  - [x] B.2a committed (`a324290`)
  - [x] B.2a migration 007 applied to production via `scripts/run-migrations.js`; verified via post-migration `pg_constraint` query
  - [x] **Side effect documented:** migration 006 also applied (runner applies all unapplied; 006 was on disk but unapplied). 006 is runtime-inert; no automatic behavior change. Do not revert without explicit safety review.
  - [x] HARD BLOCK on `db.js` and `migrations/` reinstated after B.2a commit (the lift was scoped to B.2a only)
- [x] **Phase B.2b-SL — Paper SET_STOP_LOSS DB-first caller integration in `dashboard.js` — commit `511f94e`** (closeout `e520db0`)
  - [x] Original B.2b (paper SL + paper TP) Codex implementation review = FAIL-WITH-CONDITIONS over a MEDIUM TP stale-overwrite risk (bot.js trailing/breakeven can write stale `take_profit` back to DB after rehydrate)
  - [x] Operator decision: split phase — keep SL, defer TP to a separate B.2c-TP track pending bot/dashboard TP conflict-policy design
  - [x] Implementation: new `shadowRecordManualPaperSLUpdate` wrapper in `dashboard.js` (calls `updatePositionRiskLevelsTx` + atomic `manual_sl_update` audit insert via `inTransaction`); event-id seed uses `Date.now()` + `crypto.randomBytes(4)` to avoid PK collision on repeated updates; no `position.json` or `LOG_FILE` write on the paper branch; caller throws on `!ok` (no JSON fallback)
  - [x] Live `SET_STOP_LOSS` byte-identical to pre-phase (still writes `position.json`; no DB call)
  - [x] `SET_TAKE_PROFIT` handler restored to pre-B.2b form (single shared paper+live path, `position.json` write only); `shadowRecordManualPaperTPUpdate` removed; no `manual_tp_update` references remain in `dashboard.js`
  - [x] Comment block at `dashboard.js:337–381` updated: B.2b covers paper SET_STOP_LOSS only, ratchet-up SL wording, migration 007 required for `manual_sl_update`, paper SET_TAKE_PROFIT explicitly deferred to B.2c with stale-overwrite reason
  - [x] `node --check dashboard.js` PASS
  - [x] B.2b-SL Codex re-review = PASS, safe to commit
  - [x] B.2b-SL committed (`511f94e`) — `dashboard.js` only; `bot.js`, `db.js`, `migrations/` untouched; `position.json.snap.20260502T020154Z` remained untracked
  - [x] HARD BLOCK on `dashboard.js` reinstated after B.2b-SL commit (the lift was scoped to B.2b-SL only)
- [x] **Phase B.2c-bot-preserve-TP — bot-side stale-TP overwrite fix in `bot.js` `manageActiveTrade` — commit `cc6bd2e`** (closeout `689dad4`)
  - [x] Design audit identified Option C (bot preserves DB `take_profit` when only updating SL) as the lowest-blast-radius conflict-resolution policy among five enumerated options (A=last-writer-wins, B=manual_tp_override flag, C=Option C, D=block-while-active, E=optimistic concurrency)
  - [x] B.2c-bot-preserve-TP design Codex-approved (PASS, lowest-complexity policy, `manageActiveTrade` provably never mutates `position.takeProfit`, `db.js` helper already supports partial updates)
  - [x] Implementation: payload at `bot.js:1519-1521` narrowed from `{ stop_loss, take_profit }` to `{ stop_loss }`. Comment block at `bot.js:1507-1516` documents the B.2c-bot-preserve-TP rationale and the B.2d gating relationship.
  - [x] Behavior invariants preserved: BREAKEVEN trigger (≥1.0% pnl), TRAIL trigger (≥1.5% pnl), SL calculation, `savePosition(position)` JSON write (mode-agnostic), `dbAvailable() && position.orderId` guard, fire-and-forget chain, `.catch` warn-log, `_pendingDbWrites.push`, return shape — all byte-identical to pre-phase
  - [x] Kraken execution paths untouched (no calls in this region)
  - [x] Live mode behavior preserved (`savePosition` still writes JSON for both modes; no DB write narrowing affects live since live path was always SL-only-touched here as well)
  - [x] `node --check bot.js` PASS
  - [x] B.2c-bot-preserve-TP Codex implementation review = PASS with notes
  - [x] **LOW concern (deferred):** `scripts/smoke-test-live-writes.js:225–239` wording is now stale ("active management dual-write" / "take_profit unchanged but rewritten"). Test logic remains valid because the script calls the `db.js` helper directly with both fields, and the helper still supports both-field calls. Cleanup deferred as cosmetic.
  - [x] B.2c-bot-preserve-TP committed (`cc6bd2e`) — `bot.js` only; `dashboard.js`, `db.js`, `migrations/`, and `scripts/smoke-test-live-writes.js` untouched; `position.json.snap.20260502T020154Z` remained untracked
  - [x] HARD BLOCK on `bot.js` reinstated after B.2c-bot-preserve-TP commit (the lift was scoped to B.2c-bot-preserve-TP only)
- [x] **Phase B.2d-dashboard-TP — Paper SET_TAKE_PROFIT DB-first caller integration in `dashboard.js` — commit `eca2659`** (closeout `1563310`)
  - [x] B.2d design audit produced a mirror-of-B.2b-SL design: new `shadowRecordManualPaperTPUpdate` wrapper, atomic `manual_tp_update` audit insert, no JSON fallback, throws on `!ok`, idempotency seed using `Date.now()` + `crypto.randomBytes(4)`
  - [x] B.2d design Codex-approved (PASS — bot-side prerequisite resolved by `cc6bd2e`, mirror fidelity to SL helper checked, residual TP write paths inventoried; one LOW concern about audit-row noise from repeated UI clicks, parity with SL helper)
  - [x] Implementation: new `shadowRecordManualPaperTPUpdate(dbPos, newTP)` in `dashboard.js` placed immediately after the SL wrapper. Paper `SET_TAKE_PROFIT` handler wrapped with `if (isPaper) { … }` paper-DB-first branch using `loadOpenPosition("paper")` + DB `entry_price` + `shadowRecordManualPaperTPUpdate`. Caller throws on `!ok` with operator-visible "Manual paper TP update not recorded in DB ({reason}). Take profit NOT updated." No `position.json` or `LOG_FILE` write on the paper branch.
  - [x] Live `SET_TAKE_PROFIT` byte-identical to pre-phase (existing `pos.open` guard, default pct 2.0, `pos.takeProfit` calc, `writeFileSync(POSITION_FILE, …)`, original success message)
  - [x] Comment block at `dashboard.js:337–385` updated: B.2c TP-deferral wording removed, B.2d active TP behavior documented, `cc6bd2e` cited as the bot-side prerequisite, migration 007 / `manual_tp_update` prerequisite mentioned, D-5.12 live-gate language preserved
  - [x] `node --check dashboard.js` PASS
  - [x] B.2d Codex implementation review = PASS, safe to commit, no required edits
  - [x] B.2d committed (`eca2659`) — `dashboard.js` only; `bot.js`, `db.js`, `migrations/`, and `scripts/` untouched; `position.json.snap.20260502T020154Z` remained untracked
  - [x] HARD BLOCK on `dashboard.js` reinstated after B.2d commit (the lift was scoped to B.2d only)
- [x] **Phase C — design-only audit (read-only).** Audit produced a 3-sub-phase split for the only operator-visible truth gap (manual SL/TP audit rows being write-only). Other audit targets (residual `position.json` reads, reconciliation playbook, bot.js rehydrate vs new TP write surface) confirmed safe / no-action-needed. Smoke-test wording cleanup kept as a separate deferred LOW/cosmetic phase. Sub-phases: C.1 (db.js read filter), C.2 (dashboard.js mapper + UI rendering), C.3 (`scripts/recovery-inspect.js` heuristic refinement).
- [x] **Phase C.1 — `db.js` `loadRecentTradeEvents` event-type filter expansion — commit `d0c8817`** (closeout `a967a12`)
  - [x] C.1 design audit determined the smallest safe wedge: literal-only `WHERE … IN (…)` expansion at `db.js:422`. No SELECT / JOIN / ORDER BY / LIMIT / signature / migration changes.
  - [x] C.1 design Codex-approved (PASS — minimal change, single caller `dashboard.js:656`, no live-mode regression possible because dashboard wrappers hard-code `mode: "paper"`; two LOW concerns acknowledged: external monitoring dependencies cannot be ruled out from repo search alone; heavy SL/TP-edit sessions could transiently push lifecycle events past LIMIT 30 until C.2 lands)
  - [x] Implementation: WHERE clause at `db.js:425` extended from 6 to 8 event types. Added literals are exactly `'manual_sl_update'` and `'manual_tp_update'`. Original six (`buy_filled`, `exit_filled`, `manual_buy`, `manual_close`, `reentry_buy`, `reentry_close`) preserved byte-identical in original order. Comment block at `db.js:406-411` updated to cite Phase C.1 and document inclusion of manual SL/TP audit rows for dashboard visibility.
  - [x] Behavior invariants preserved: function signature `loadRecentTradeEvents(mode, limit = 30)`, `_requireMode` validation, SELECT column list, LEFT JOIN, `WHERE te.mode = $1`, `ORDER BY te.timestamp DESC`, `LIMIT $2`, bind params `[mode, limit]` — all byte-identical to pre-phase
  - [x] No write behavior changed: `insertTradeEvent`, `updatePositionRiskLevels`, `updatePositionRiskLevelsTx`, `loadOpenPosition`, all other helpers unchanged. Helper remains SELECT-only.
  - [x] No migration required (migration 007 from `a324290` already extended the CHECK constraint)
  - [x] No live trading behavior change (no live row of the new event types can exist; `mode = 'live'` query results unchanged)
  - [x] `node --check db.js` PASS
  - [x] C.1 Codex implementation review = PASS with notes
  - [x] **LOW concern (resolved by C.2 audit):** C.1 design report mentioned a `dec_blocked` CSS class for the rough-rendering window, but the actual class name in `dashboard.js` is `mode-blocked`. C.2 audit verified canonical class names (`mode-paper` / `mode-live` / `mode-blocked` / `dec-buy` / `dec-exit` / `dec-blocked`) and resolved the discrepancy.
  - [x] C.1 committed (`d0c8817`) — `db.js` only; `dashboard.js`, `bot.js`, `migrations/`, and `scripts/` untouched; `position.json.snap.20260502T020154Z` remained untracked
  - [x] HARD BLOCK on `db.js` reinstated after C.1 commit (the lift was scoped to C.1 only)
  - [x] **Rough-rendering window opened by C.1 — now closed by C.2 (`2d10107`):** SL/TP audit rows are admitted by the read filter and rendered via a dedicated panel.
- [x] **Phase C.2 — `dashboard.js` mapper + dedicated "Recent Risk Edits" panel — commit `2d10107`** (closeout `1372392`)
  - [x] C.2 audit re-scoped the visibility surface: main `/dashboard` `renderTradeTable` is CSV-fed (not affected by C.1), latest-decision badge is JSON-log-fed (not affected by C.1). The only DB-fed consumer of the C.1 payload is `renderPerfTrades` in the Performance tab, which already filters to `type === "EXIT"` and silently drops SL/TP audit rows. C.2 chose Option B (dedicated panel) over Option A (mix into Performance Recent Trades) and Option C (shape-only no UI).
  - [x] C.2 design Codex-approved with required edits (PASS-WITH-EDITS — Option B isolates audit-only rows; two required edits: MEDIUM "displayed window" caveat in the panel sublabel because LIMIT 30 is shared with EXIT rows, LOW Order ID escaping via `btEsc()` in the new renderer)
  - [x] Implementation: `_dbTradeEventToLegacyShape` switch (`dashboard.js:584-594`) extended with `manual_sl_update` → `SL_UPDATE` and `manual_tp_update` → `TP_UPDATE` cases; legacy shape (`dashboard.js:604-635`) extended with four metadata-backed fields `oldStopLoss` / `newStopLoss` / `oldTakeProfit` / `newTakeProfit` (parseFloat-coerced, null-safe; null for unrelated rows); raw `metadata` not exposed.
  - [x] New CSS rule `.perf-risk-edits-card` (`dashboard.js:9545-9548`) mirrors `.perf-trades-card` family.
  - [x] New HTML markup (`dashboard.js:9873-9881`): `<div class="card perf-risk-edits-card">` with title `🛡️ Recent Risk Edits — manual SL/TP updates`, sublabel including the required LIMIT 30 caveat verbatim, and `<div id="pf-risk-edits-body">`.
  - [x] New `renderPerfRiskEdits()` function (`dashboard.js:10685-10728`): filters `data.recentTrades` to `type === "SL_UPDATE" || type === "TP_UPDATE"`; columns Time, Type, Old, New, Order ID; type label fixed string ("SL update" / "TP update", no emoji); Old/New formatted as `"$X.XXXX"` via `Number.isFinite` + `toFixed(4)`; "—" for null; Order ID escaped via `btEsc()`; Time also `btEsc(timeAgo(…))`-wrapped; empty state `"No recent SL/TP edits yet."`.
  - [x] Wiring: `renderPerformance()` (`dashboard.js:11032`) calls `renderPerfRiskEdits()` immediately after `renderPerfTrades()`. `renderPerformanceLoading()` (`dashboard.js:10532-10534`) sets `pf-risk-edits-body` to `"Loading…"` on segment switch.
  - [x] Required edits both present: (1) MEDIUM LIMIT 30 caveat verbatim at `dashboard.js:9879`; (2) LOW Order ID escaping at `dashboard.js:10723`.
  - [x] Behavior invariants preserved: latest-decision badge unchanged, `fired` counter unchanged, P&L aggregates unchanged, win-loss aggregates unchanged, LOG_FILE behavior unchanged (no JSON write added; B.2 paper-mode discipline preserved), `renderTradeTable` (CSV-fed) unchanged, live trading behavior unchanged, Kraken execution untouched
  - [x] `node --check dashboard.js` PASS
  - [x] C.2 Codex implementation review = PASS, all 41 checklist items PASS, no required edits
  - [x] C.2 committed (`2d10107`) — `dashboard.js` only; `bot.js`, `db.js`, `migrations/`, and `scripts/` untouched; `position.json.snap.20260502T020154Z` remained untracked
  - [x] HARD BLOCK on `dashboard.js` reinstated after C.2 commit (the lift was scoped to C.2 only)
- [x] **Phase C.3 — `scripts/recovery-inspect.js` heuristic refinement — commit `1a16dd8`**
  - [x] C.3 audit confirmed: dashboard wrappers (`shadowRecordManualPaperSLUpdate` / `shadowRecordManualPaperTPUpdate`) skip `insertTradeEvent` on `!positionId`, so a null-FK `manual_sl_update` / `manual_tp_update` row cannot be produced by current code paths. The current `_attempt$` regex misclassifies these audit-only types as "suspicious — review" if they ever appear; refinement is operator-playbook clarity, not correctness.
  - [x] C.3 design Codex-approved with required edits (PASS-WITH-EDITS — 3-way classification: `_attempt$` / audit-only / suspicious; one LOW required wording edit: replace "review if non-zero" with "investigate if seen" because the GROUP BY only emits rows that exist, so the "non-zero" framing is tautological)
  - [x] Implementation: replace the 1-line ternary at `scripts/recovery-inspect.js:159` with a 3-way `if/else if/else` block (`scripts/recovery-inspect.js:165-173`). New function-local `const AUDIT_ONLY_EVENT_TYPES = new Set(["manual_sl_update", "manual_tp_update"])` declared at `scripts/recovery-inspect.js:164`. New 6-line Phase C.3 comment block at `scripts/recovery-inspect.js:158-163` explaining the wrapper-skip rationale and operator-playbook intent.
  - [x] Required edit present: approved phrase "audit-only — investigate if seen" at `scripts/recovery-inspect.js:170`. Rejected phrase "audit-only — review if non-zero" is absent.
  - [x] Behavior invariants preserved: SQL query at `scripts/recovery-inspect.js:145-152` (SELECT, WHERE, GROUP BY, ORDER BY, bind params) byte-identical; SAFETY CONTRACT at `scripts/recovery-inspect.js:10-21` unchanged; function signature `async function showNullFkTradeEvents(mode)` unchanged; the empty-rows early-return at `scripts/recovery-inspect.js:154-157` unchanged; per-row `console.log` print pattern unchanged. `_attempt$` label "(expected — failed attempt)" preserved verbatim. Default fallback "(suspicious — review)" preserved verbatim.
  - [x] No bot/dashboard/runtime/DB-write/Kraken/trading behavior changed (the script remains read-only per its SAFETY CONTRACT; bot.js and dashboard.js do not import this script)
  - [x] `node --check scripts/recovery-inspect.js` PASS
  - [x] C.3 Codex implementation review = PASS, all checklist items PASS, no required edits
  - [x] **Codex non-blocking notes (informational, not required edits):** (a) any in-repo runbooks quoting the old classification wording verbatim should be updated if found; (b) future audit-only event types must be manually added to `AUDIT_ONLY_EVENT_TYPES` (no mechanism enforces this — track at the migration-author level).
  - [x] C.3 committed (`1a16dd8`) — `scripts/recovery-inspect.js` only; `dashboard.js`, `bot.js`, `db.js`, `migrations/`, `scripts/smoke-test-live-writes.js`, and all other `scripts/` files untouched; `position.json.snap.20260502T020154Z` remained untracked
  - [x] HARD BLOCK on `scripts/recovery-inspect.js` reinstated after C.3 commit (the lift was scoped to C.3 only)

## Full B.2 paper-mode dual-truth track — closed

All paper-mode write paths are now DB-canonical:

| Path | DB-canonical since | Commit |
|---|---|---|
| Paper BUY (BUY_MARKET / OPEN_LONG) | Phase A.2 | `959fef7` |
| Paper CLOSE_POSITION | Phase B.1 | `cb7facb` |
| Paper SELL_ALL | Phase B.1 | `cb7facb` |
| Paper SET_STOP_LOSS | Phase B.2b-SL | `511f94e` |
| Paper SET_TAKE_PROFIT | Phase B.2d-dashboard-TP | `eca2659` |

Live-mode write paths remain `position.json`-only by design until **Phase D-5.12** (Live persistence gate lift). The only currently active remaining dual-truth surface in the system is the live-mode write path, which is intentional.

- [x] **Smoke-test wording cleanup — `scripts/smoke-test-live-writes.js` four-site wording refresh — commit `735b10f`**
  - [x] Audit identified four stale wording sites: file-header coverage table at line 11, Step 3 banner at line 225, Step 3 operator-visible label at line 226, Step 3 assertion message at line 238. The "active management dual-write" / "take_profit unchanged but rewritten" wording had been inaccurate since B.2c-bot-preserve-TP narrowed bot.js's manage-update payload to `{ stop_loss }` only.
  - [x] Codex design review = APPROVE (4-site wording-only refresh; three LOW-severity informational concerns about archival prose, line-11 length, and speculative label-string matchers — none blocking)
  - [x] Implementation: line 11 → `//   3. updatePositionRiskLevels — direct helper test (D-5.10.5.3 helper; bot.js now SL-only post-B.2c).`; line 225 → `// ── Step 3: updatePositionRiskLevels — direct helper test (both fields) ────`; line 226 → `"updatePositionRiskLevels (D-5.10.5.3) — open row, SL→entry, helper both-field path"`; line 238 → `"take_profit round-trips through helper both-field path"`.
  - [x] Behavior invariants preserved: helper call (`updatePositionRiskLevels("live", SMOKE_ORDER_ID, { stop_loss, take_profit })`) byte-identical, fixture constants unchanged, all assertion booleans on lines 231/237/238/239 unchanged, cleanup logic unchanged, exit-code behavior unchanged (0/1/2), imports unchanged, SAFETY framing at lines 23–25 unchanged
  - [x] No bot/dashboard/db/migration/runtime behavior changed
  - [x] `node --check scripts/smoke-test-live-writes.js` PASS
  - [x] Codex implementation review = PASS with notes (four wording sites match the approved design verbatim; three LOW-severity informational notes — none blocking)
  - [x] Committed (`735b10f`) — `scripts/smoke-test-live-writes.js` only; `dashboard.js`, `bot.js`, `db.js`, `migrations/`, `scripts/recovery-inspect.js`, and all other `scripts/` files untouched; `position.json.snap.20260502T020154Z` remained untracked
  - [x] HARD BLOCK on `scripts/smoke-test-live-writes.js` reinstated after commit (the lift was scoped to this phase only)
- [x] **Phase D-5.12a — design-only live persistence gate lift review (4-iteration design refinement)**
  - [x] Read-only audit identified the five live `dashboard.js` manual handlers (OPEN_LONG/BUY_MARKET, CLOSE_POSITION, SELL_ALL, SET_STOP_LOSS, SET_TAKE_PROFIT) and their JSON-only persistence; bot.js autonomous live writes are mode-aware via `_modeFromConfig()` and DO write to Postgres; the dual-truth gap is specifically dashboard-driven manual live actions.
  - [x] v1 design report produced; Codex review = PASS-WITH-EDITS (5 HIGH/MEDIUM concerns: manual gating before DB wiring, P0-L3 `positions_one_open_per_mode_idx` race escalation, fail-loud DB-failure default, SELL_ALL split, helper-strategy decision; plus three hidden risks added: `/api/control` mode-switch race, `execKrakenOrder` retry semantics, recovery-inspect/reconciliation-shadow operator-playbook gaps)
  - [x] v2 design report produced; Codex review = PASS-WITH-EDITS (4 required: emergency-audit double-fault fallback, MANUAL_LIVE_ARMED two-layer check, /api/control TOCTOU lock-before-read, D-5.12d-to-D-5.12h interim-state policy; plus 2 MEDIUM schema hardenings: event_id UNIQUE + mode CHECK)
  - [x] v3 design report produced; Codex review = PASS-WITH-EDITS (5 required: canonical event_id recipe, staged-rollout all-handler smoke checklist, triple-fault stderr reconstruction contract, operator decision #8 added, D-5.12h smoke-harness scope clarified; plus 5 minor concerns folded in)
  - [x] **v4 design report produced; Codex review = PASS WITH NOTES — design ready for operator decision gates.** All v3 required edits incorporated; all 5 minor concerns folded in; 9 LOW operational concerns flagged (none blocking).
  - [x] Operator accepted all 8 design decision defaults (recorded in `STATUS.md` "Operator decision defaults — accepted for D-5.12" section).
  - [x] D-5.12 sub-phase plan approved: D-5.12b (manual live gating) → D-5.12c (live helper wrappers + emergency_audit_log migration 008) → D-5.12d (live OPEN_LONG persistence) → D-5.12e (live CLOSE_POSITION persistence) → D-5.12f (live SELL_ALL persistence) → D-5.12g (live SL/TP persistence) → D-5.12h (live rehydrate + recovery scripts + smoke harness + rollback plan) → D-5.12i (closeout docs).
  - [x] **No code, no commits, no migrations, no deploys, no edits to dashboard.js / bot.js / db.js / scripts/ / migrations/ during D-5.12a review.** All HARD BLOCK files remained blocked. Working tree clean except untracked `position.json.snap.20260502T020154Z`.
  - [x] 9 LOW operational concerns from Codex v4 carried forward to downstream sub-phase docs (see `STATUS.md` "Codex v4 LOW operational notes" section): canonical event_id precision, retry_history growth/retention, all-handler smoke deploy-velocity, Railway stderr retention finite, process-local lock check/set must avoid async yield, D-5.12h testcontainer infrastructure scope, emergency audit latency budget, deploy warning is documentation-only, emergency rows retention/export format.
- [x] **Phase D-5.12b — manual live gating implementation in `dashboard.js` — commit `24246d8`**
  - [x] Scoped `dashboard.js` HARD BLOCK lift authorized by operator
  - [x] New `MANUAL_LIVE_COMMANDS` Set (`dashboard.js:90-97`) holds the six manual-live action tokens: `BUY_MARKET`, `OPEN_LONG`, `CLOSE_POSITION`, `SELL_ALL`, `SET_STOP_LOSS`, `SET_TAKE_PROFIT`
  - [x] Layer 1 `MANUAL_LIVE_ARMED` gate at `/api/trade` POST entry (`dashboard.js:12247-12282`): rejects with HTTP 403 when `MANUAL_LIVE_COMMANDS.has(body.command) && ctrlNow.paperTrading === false && process.env.MANUAL_LIVE_ARMED !== "true"`. Paper mode unaffected.
  - [x] Layer 2 `MANUAL_LIVE_ARMED` gate inside `handleTradeCommand` (`dashboard.js:1449-1464`) after `const isPaper = ctrl.paperTrading !== false;`: throws when `!isPaper && process.env.MANUAL_LIVE_ARMED !== "true"`. Defense-in-depth catches any future code path that reaches `handleTradeCommand` without going through `/api/trade`. Paper mode unaffected.
  - [x] `/api/control` SET_MODE lock-before-read (`dashboard.js:12124-12137`): `_release = acquireTransitionLock(command)` moved to BEFORE the position.json / control-state preflight reads (was previously after); the lock primitive at `dashboard.js:11380-11385` is synchronous (no async yield between check and set), satisfying Codex v4 note 9.f.
  - [x] Codex round-1 implementation review = FAIL [MEDIUM] over a stale outer ctrl snapshot consumed by the SET_MODE preflight after lock acquisition; required edit was Codex's own option (b): scoped under-lock re-read of `CONTROL_FILE` inside the SET_MODE branch
  - [x] Revision applied: scoped `CONTROL_FILE` re-read at `dashboard.js:12142-12157` after lock acquisition; the outer pre-lock `ctrl` snapshot at `dashboard.js:12092-12109` is intentionally retained for non-SET_MODE `requiresConfirm` logic and was NOT moved
  - [x] Codex round-2 implementation review = **PASS, safe to commit.** All 34 sub-items PASS: required fix verified (outer pre-lock retained, SET_MODE re-reads under lock, ctrl.killed and writeFileSync use locked snapshot); MANUAL_LIVE_COMMANDS exact-set verified; both gates fail-closed; acquireTransitionLock synchronous; no Kraken / fetchKrakenBalance / position.json / DB-write / helper / emergency_audit_log / Migration 008 / SL-TP / breakeven / trailing-stop / `/api/run-bot` lock changes; paper-mode behavior unchanged; only `dashboard.js` changed; snap remained untracked.
  - [x] `node --check dashboard.js` PASS
  - [x] D-5.12b committed (`24246d8`) — `dashboard.js` only (76 insertions, 6 deletions); `bot.js`, `db.js`, `migrations/`, and `scripts/` untouched; `position.json.snap.20260502T020154Z` remained untracked
  - [x] HARD BLOCK on `dashboard.js` reinstated after D-5.12b commit (the lift was scoped to D-5.12b only)
  - [x] **Out of scope for D-5.12b (deferred to later sub-phases):** DB persistence, helper wrappers (`shadowRecordManualLive*`), `emergency_audit_log` table, Migration 008, Kraken execution path changes, SL/TP/breakeven/trailing-stop logic changes, live rehydrate enable, smoke harness — all D-5.12c through D-5.12h
- [x] **Phase D-5.12c — live helper wrappers + Migration 008 (`emergency_audit_log`) + `db.js` emergency-audit insert helper + dashboard.js emergency utilities — commit `4ae3689`**
  - [x] Scoped `dashboard.js` + `db.js` + `migrations/` HARD BLOCK lifts authorized by operator
  - [x] D-5.12c design v1 = Codex PASS-WITH-REQUIRED-EDITS (3 HIGH: SQL contract `DO NOTHING` vs `DO UPDATE` contradiction, helper-vs-caller emergency-audit responsibility, P0-L3 unique-violation claim wrong; 1 MEDIUM: retry_history concrete storage; 2 PASS-with-notes: `attempted_payload_hash` dual placement, redaction guard)
  - [x] D-5.12c design v2 = **Codex PASS, design ready for operator scoped-file approval** (all 3 HIGH edits resolved + MEDIUM concrete + 2 PASS-with-notes folded; all 9 prior Codex Q-rulings preserved)
  - [x] `migrations/008_emergency_audit_log.sql` created on disk (46 lines): operator-warning header forbidding runner application without explicit authorization; `CREATE TABLE IF NOT EXISTS emergency_audit_log` with all 13 columns (`id BIGSERIAL PRIMARY KEY`, `event_id TEXT NOT NULL UNIQUE`, `timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `mode TEXT NOT NULL CHECK (mode IN ('paper','live'))`, `source TEXT NOT NULL`, `kraken_order_id TEXT`, `failure_class TEXT NOT NULL`, `error_message TEXT`, `attempted_payload JSONB NOT NULL DEFAULT '{}'::jsonb`, `resolved_at TIMESTAMPTZ`, `resolved_by TEXT`, `resolution_notes TEXT`, `metadata JSONB NOT NULL DEFAULT '{}'::jsonb`); four `IF NOT EXISTS` indexes (unresolved partial, kraken_order_id partial, source/timestamp, failure_class/timestamp); forward-only / idempotent
  - [x] `db.js` additions (+118 lines): `_canonicalJsonSorted(value)` private helper (RFC-8785-style), `sha256HexCanonical(obj)` exported, `buildEmergencyEventId(allowlistedPayload)` exported with `d512-emergency-` prefix, `classifyDbError(err)` exported (returns `unique_violation_one_open_per_mode` / `unique_violation_kraken_order_id` / `other`), `insertEmergencyAuditLog(client, event)` exported with `ON CONFLICT (event_id) DO UPDATE` appending `metadata.retry_history` via `jsonb_set` and preserving first-attempt `attempted_payload` and `error_message` via `COALESCE`
  - [x] `dashboard.js` additions (+379 lines): four live wrappers `shadowRecordManualLive{Buy,Close,SLUpdate,TPUpdate}` mirroring paper helper return contract `{ ok, reason, error?, errorClass?, emergency_context? }` with `mode: "live"` hard-coded, `metadata.source` tagged `manual_live_*` (paper tags unchanged); `_redactAttemptedPayload(rawObj)` recursive redaction (regex `/secret|key|token|cookie|auth|signature|password|credential|nonce/i`); `_loglineFallback(line)` (LOG_FILE append + stderr `triple_fault` fallback); `_emergencyAuditWrite(failureContext)` (fresh `inTransaction`, defensive re-redact and re-hash, `attempted_payload_hash` written into both event_id allowlist AND `attempted_payload` top-level)
  - [x] Helpers remain unused at end-of-phase: no `/api/trade` handler call-site references any `shadowRecordManualLive*`. Live handler wiring is D-5.12d/e/f/g.
  - [x] D-5.12c implementation review round 1 = FAIL [MEDIUM] over `classifyDbError` constraint-name mismatch: matched the Postgres-default `positions_kraken_order_id_key`, but the actual constraint declared in `migrations/002_trade_events_positions.sql:66-67` is `positions_kraken_order_unique`
  - [x] Revision applied: literal-string swap in `db.js` `classifyDbError` from `positions_kraken_order_id_key` to `positions_kraken_order_unique`. No other behavior changed.
  - [x] D-5.12c implementation review round 2 = **Codex PASS, safe to commit** (all 22 sub-items PASS post-revision)
  - [x] `node --check db.js` PASS; `node --check dashboard.js` PASS
  - [x] D-5.12c committed (`4ae3689`) — 3 files (dashboard.js + db.js + migrations/008_emergency_audit_log.sql) / 543 insertions / 0 deletions; `bot.js`, `scripts/` untouched; `position.json.snap.20260502T020154Z` remained untracked
  - [x] HARD BLOCK on `dashboard.js`, `db.js`, `migrations/` reinstated after D-5.12c commit (the lifts were scoped to D-5.12c only)
  - [x] **Migration 008 NOT APPLIED to production.** Application gated by separate explicit operator authorization. Migration file's header forbids running via `scripts/run-migrations.js` without authorization.
  - [x] **Out of scope for D-5.12c (deferred to later sub-phases):** wiring the four live wrappers into live `OPEN_LONG` / `CLOSE_POSITION` / `SELL_ALL` / `SET_STOP_LOSS` / `SET_TAKE_PROFIT` handlers (D-5.12d/e/f/g); P0-L3 `positions_one_open_per_mode_idx` recovery branch (D-5.12d); live rehydrate (D-5.12h); smoke harness (D-5.12h); recovery-script updates (D-5.12h); operator playbook (D-5.12i); Migration 008 production application (separate authorization gate)
- [x] **Phase D-5.12d — live OPEN_LONG / BUY_MARKET persistence wired through the failure ladder — commit `1c20177`**
  - [x] D-5.12d design v1 = Codex PASS-WITH-REQUIRED-EDITS (1 HIGH: Migration 008 application must be a hard prerequisite for production deploy, not a recommendation; 1 PASS-with-notes: implementation review must verify no raw payload path enters `_loglineFallback`)
  - [x] D-5.12d design v2 = **Codex PASS, design ready for scoped `dashboard.js` implementation** (Section 4 hardened so Migration 008 application + verification is a HARD prerequisite for production deploy; production sequencing tightened to mandatory order: code commit → closeout → apply+verify Migration 008 → deploy → first live BUY)
  - [x] Scoped `dashboard.js` HARD BLOCK lift authorized by operator
  - [x] Live OPEN_LONG / BUY_MARKET branch (`dashboard.js:1895-2040`): replaces the pre-existing JSON-only live BUY path (two `writeFileSync` calls + LOG_FILE append) with a DB-first failure-ladder block that calls `shadowRecordManualLiveBuy(entry, newPos)` after `execKrakenOrder` returns.
  - [x] On helper `{ ok: true }`: `writeFileSync(POSITION_FILE, newPos)` after DB success; best-effort try/catch LOG_FILE append; `balanceCache = null` invalidation; existing return shape preserved.
  - [x] On helper `{ ok: false }`: maps `r.errorClass === "unique_violation_one_open_per_mode"` → `failure_class: "kraken_post_success_db_unique_violation"`; otherwise → `"kraken_post_success_db_other_error"`. No `position.json` write. No LOG_FILE success-row append. No Kraken cancellation. No auto-retry.
  - [x] Calls `_emergencyAuditWrite(failureContext)` (Layer 2). On audit success: structured `log.error` with P0-L3 distinction or generic. On audit failure: builds JSON reconstruction line with `auditResult.event_id ?? null`, `attempted_payload`, `attempted_payload_hash`, `prior_failures`, timestamp; calls `_loglineFallback(line)` (Layer 3 → LOG_FILE; Layer 4 stderr triple-fault floor inside `_loglineFallback`).
  - [x] Operator-visible error thrown — pointer-only message, does NOT include `attempted_payload`. Distinct messages for the unique-violation and generic-DB-error cases.
  - [x] D-5.12d implementation review round 1 = FAIL [HIGH] over the helper's early-return paths (`db_unavailable` / `validation_failed`) defaulting `failureContext.attempted_payload` to `{}`, losing reconstruction context exactly when audit recovery is most critical
  - [x] Revision applied: helper-provided `r.emergency_context` is preferred when present; otherwise the call-site computes a fallback `attempted_payload` locally from `newPos` via `_redactAttemptedPayload(...)` with the helper-matching field shape (symbol, side, entry_price, entry_time, quantity, trade_size_usd, leverage, effective_size_usd, stop_loss, take_profit, kraken_order_id), and `attempted_payload_hash = sha256HexCanonical(attempted_payload)`. `failureContext.attempted_payload` and Layer-3 reconstruction line both use the resolved values; no `{}` default remains in the live-BUY failure path.
  - [x] D-5.12d implementation review round 2 = **Codex PASS, safe to commit** (all 21 sub-items PASS post-revision; helper-provided / locally-redacted dual path verified; no raw payload entering `_loglineFallback`)
  - [x] `node --check dashboard.js` PASS
  - [x] D-5.12d committed (`1c20177`) — `dashboard.js` only (146 insertions, 3 deletions); `bot.js`, `db.js`, `migrations/`, `scripts/` untouched; `position.json.snap.20260502T020154Z` remained untracked
  - [x] HARD BLOCK on `dashboard.js` reinstated after D-5.12d commit (the lift was scoped to D-5.12d only)
  - [x] **Migration 008 NOT APPLIED to production.** Per accepted v2 D-5.12d design: HARD prerequisite before any D-5.12d production deploy or live production exercise. Verification step required: `SELECT 1 FROM emergency_audit_log LIMIT 1;` returns successfully.
  - [x] **Out of scope for D-5.12d (deferred to later sub-phases):** wiring the four live wrappers into live `CLOSE_POSITION` / `SELL_ALL` / `SET_STOP_LOSS` / `SET_TAKE_PROFIT` handlers (D-5.12e/f/g); live rehydrate (D-5.12h); smoke harness (D-5.12h); recovery-script updates (D-5.12h); operator playbook (D-5.12i); Migration 008 production application (separate authorization gate); D-5.12d production deployment (separate authorization gate); first live BUY exercise (separate authorization gate, requires `MANUAL_LIVE_ARMED="true"`).
- [x] **Phase D-5.12e — live CLOSE_POSITION persistence wired through the failure ladder — commit `95f39f2`**
  - [x] D-5.12e design v1 = Codex PASS-WITH-REQUIRED-EDITS (1 MEDIUM: `balanceCache` invalidation must lock to option (b) — every post-Kraken outcome including DB persistence failure)
  - [x] D-5.12e design v2 = **Codex PASS, design ready for scoped `dashboard.js` implementation** (`balanceCache = null` locked to fire on success path AND every DB-failure class, placed BEFORE the operator-visible throw and OUTSIDE the `auditResult.ok` if/else logging branch)
  - [x] Scoped `dashboard.js` HARD BLOCK lift authorized by operator
  - [x] Live CLOSE_POSITION branch (`dashboard.js:2087-2218`): replaces the pre-existing JSON-only live CLOSE path (which used stale `pos` from `position.json`, called Kraken sell against `pos.quantity`, then unconditionally wrote LOG_FILE + `position.json {open: false}`) with a DB-first failure-ladder block.
  - [x] Pre-Kraken `loadOpenPosition("live")` is the canonical source. Throws "No open live position to close" (BEFORE Kraken executes) if null.
  - [x] Sell volume = `parseFloat(dbPos.quantity).toFixed(8)` (DB-canonical). No Kraken balance check added.
  - [x] PnL computed from DB `entry_price` and `trade_size_usd`. `exitEntry` built with `paperTrading: false` and `orderId` from Kraken response.
  - [x] After Kraken success, calls `shadowRecordManualLiveClose(exitEntry)`.
  - [x] On helper `{ ok: true }`: `writeFileSync(POSITION_FILE, { open: false })` after DB success; best-effort try/catch LOG_FILE append; `balanceCache = null` invalidation; existing return shape preserved.
  - [x] On helper `{ ok: false }`: maps `r.reason === "no_open_position"` → `failure_class: "kraken_post_success_db_no_open_position"`; otherwise → `"kraken_post_success_db_other_error"`. No `position.json` write. No LOG_FILE success-row append. No Kraken cancellation. No auto-retry.
  - [x] Calls `_emergencyAuditWrite(failureContext)` (Layer 2). On audit success: structured `log.error` with `no_open_position` distinction or generic. On audit failure: builds JSON reconstruction line with `auditResult.event_id ?? null`, `attempted_payload`, `attempted_payload_hash`, `prior_failures`, timestamp; calls `_loglineFallback(line)` (Layer 3 → LOG_FILE; Layer 4 stderr triple-fault floor).
  - [x] Helper-provided `r.emergency_context` preferred when present. Otherwise call-site computes fallback `attempted_payload` from `dbPos` + `exitEntry` values via `_redactAttemptedPayload(...)` with the helper-matching field shape (9 fields: `symbol: exitEntry.symbol`, `exit_price`, `exit_time`, `exit_reason`, `quantity`, `trade_size_usd`, `realized_pnl_usd`, `realized_pnl_pct`, `kraken_exit_order_id`), `attempted_payload_hash = sha256HexCanonical(attempted_payload)`, hash injected post-canonicalization.
  - [x] **Per Codex v2 design ruling option (b):** `balanceCache = null` runs on EVERY post-Kraken outcome — success path AND every DB-failure class — placed AFTER audit/log work and BEFORE the conditional throws (OUTSIDE the `auditResult.ok` if/else logging branch).
  - [x] Operator-visible error thrown — pointer-only message, does NOT include `attempted_payload`. Distinct messages for `no_open_position` (race / drift) and generic-DB-error cases.
  - [x] D-5.12e implementation review round 1 = FAIL [HIGH] over `symbol: dbPos.symbol` in the call-site fallback. Helper-internal uses `exitEntry.symbol`; if `CONFIG.symbol` ever drifted from `"XRPUSDT"`, hashes would diverge across helper-driven and call-site-driven failure paths.
  - [x] Revision applied: targeted one-line swap `dbPos.symbol` → `exitEntry.symbol` in the fallback `attempted_payload` block. No other behavior changed.
  - [x] D-5.12e implementation review round 2 = **Codex PASS, safe to commit** (all 6 sub-items PASS post-revision; helper-driven and call-site-driven paths verified byte-stable; HIGH symbol-mismatch finding fully resolved)
  - [x] `node --check dashboard.js` PASS
  - [x] D-5.12e committed (`95f39f2`) — `dashboard.js` only (145 insertions, 10 deletions); `bot.js`, `db.js`, `migrations/`, `scripts/`, `orchestrator/` untouched; `position.json.snap.20260502T020154Z` remained untracked
  - [x] HARD BLOCK on `dashboard.js` reinstated after D-5.12e commit (the lift was scoped to D-5.12e only)
  - [x] **Migration 008 NOT APPLIED to production.** Per accepted v2 D-5.12d design: HARD prerequisite before any D-5.12d/e production deploy or live exercise. Verification step required: `SELECT 1 FROM emergency_audit_log LIMIT 1;` returns successfully.
  - [x] **Out of scope for D-5.12e (deferred to later sub-phases):** wiring the live wrappers into `SELL_ALL` / `SET_STOP_LOSS` / `SET_TAKE_PROFIT` handlers (D-5.12f/g); live rehydrate (D-5.12h); smoke harness (D-5.12h); recovery-script updates (D-5.12h); operator playbook (D-5.12i); Migration 008 production application (separate authorization gate); D-5.12d/e production deployment (separate authorization gate); first live BUY / live CLOSE exercise (separate authorization gate, requires `MANUAL_LIVE_ARMED="true"`).

## Active phase

- [~] **Hands-Free ARC track — ARC-0 PASS, ARC-1 closed (`95157ae`), ARC-2 closed (`3b53223`), ARC-3 closed (`9b5093f`), ARC-4 closed (`afd5930`), ARC-5 closed (`698e0d3`), ARC-6 closed (`9eff3c9`), N-1 PASS, N-1 closeout sync closed (`4d472e2`), N-1.5 design review PASS, ARC-7 closed (`8266db2`), N-2 design review PASS WITH REQUIRED EDITS, N-2b closed (`e6c9189`), N-2c closed (`9ae139d`), N-3 attempt 2026-05-03 HALTED (runner exit 1; `getaddrinfo ENOTFOUND` for Railway-internal Postgres hostname; no SQL executed; production-DB unchanged; Migration 008 NOT applied; prior Victor approval consumed), N-2d closed (`3732721`), N-2e closed (`afe94d1`), N-2f closed (`3af1e44`), N-2g closed (`926eb7f`), N-2h closed (`ea7774d`), N-2i closed (`5ee1dcb`), N-2j closed (`548383b`), N-2k in progress (Phase N-2k — mode: DOCS-ONLY).**
  - [x] **ARC-0 — Current State Verification (read-only) — PASS (2026-05-02).** HEAD on `c13789f`; only untracked `position.json.snap.20260502T020154Z`; no protected file modifications; D-5.12e closeout commit current; Migration 008 file present on disk and documented as NOT applied to production. No edits, no commits, no migrations, no deploys.
  - [x] **ARC-1 — Orchestrator Protection Matrix (docs-only) — CLOSED, committed `95157ae` (2026-05-02).** New `orchestrator/PROTECTED-FILES.md` (per-path SAFE / RESTRICTED / HARD BLOCK matrix, Codex review and operator approval requirements, stop conditions, must-never-be-automated list, Ruflo / future-automation auto-approve prohibition; 156 lines after Codex required edits applied). `orchestrator/APPROVAL-GATES.md` intentionally left unchanged in ARC-1 (deferred to ARC-2). Minimal ARC-1 entries in `STATUS.md` / `CHECKLIST.md` / `NEXT-ACTION.md`. Codex docs-only review round 1 = PASS WITH REQUIRED EDITS (3 wording fixes: SAFE rule for safety-policy docs, split SAFE approval-gate row, clarify operational-action rows so production execution is operator-only AND plan/checklist/runbook requires Codex review before operator approval). Round 2 (post-edits) = PASS. Operator approved. Committed `95157ae` — 4 files / +173 / 0; no runtime / protected file touched.
  - [x] **ARC-2 — Approval Gate Matrix (docs-only) — CLOSED, committed `3b53223` (2026-05-02).** `orchestrator/APPROVAL-GATES.md` substantially expanded (67 → 192 lines): 16-gate numbered matrix (bot.js / db.js / migration file / production migration application / Railway deploy / live Kraken / live dashboard handler / position.json write / real-money / Ruflo install / Claude Code permission / production-state mutation / production env or secret / MANUAL_LIVE_ARMED / destructive git / automation widening), "actions that do NOT need operator approval", "Codex review BEFORE operator approval", "what an approval must include" structured form, "what is NOT operator approval" non-equivalence list, "production actions require separate approval" separation rule, refreshed stop conditions, cross-references to `PROTECTED-FILES.md`. Original five sections preserved. `PROTECTED-FILES.md` left unchanged. Codex docs-only review round 1 = PASS WITH REQUIRED EDITS (1 wording fix: stale "Current allowed next action" pointer in STATUS.md). Round 2 (post-fix) = PASS. Operator approved. Committed `3b53223` (4 files, +148 / -21); no runtime / protected file touched.
  - [x] **ARC-3 — Phase Mode System (docs-only) — CLOSED, committed `9b5093f` (2026-05-02).** New `orchestrator/PHASE-MODES.md` (236 lines) defines six phase modes (READ-ONLY AUDIT, DESIGN-ONLY, DOCS-ONLY, SAFE IMPLEMENTATION, HIGH-RISK IMPLEMENTATION, PRODUCTION ACTION) with allowed/blocked actions, required reviews, required approvals, and stop conditions per mode. Phase-labeling rule (every future phase must be labeled `Phase <name> — mode: <MODE NAME>` before work begins), ambiguous-mode rule (stop and ask; higher-risk fallback), and automation/Ruflo non-promotion rule established. Path A follow-up applied Codex-required safety-policy cross-reference wording edits to `orchestrator/PROTECTED-FILES.md` and `orchestrator/APPROVAL-GATES.md` so the new safety-policy doc is registered. Codex docs-only review round 1 = PASS WITH REQUIRED EDITS (6 wording fixes including PHASE-MODES.md registration in both registries and a PRODUCTION ACTION precondition). Round 2 = PASS WITH REQUIRED EDITS (6 status-doc wording fixes to reflect Path A six-file scope). Round 3 (post-fixes) = PASS. Operator approved. Committed `9b5093f` (6 files, +258 / -17); no runtime / protected file touched.
  - [x] **ARC-4 — Next-Action Selector (Phase ARC-4 — mode: DOCS-ONLY) — CLOSED, committed `afd5930` (2026-05-03).** New `orchestrator/NEXT-ACTION-SELECTOR.md` (108 lines) defines ten ordered selector rules and a master order (ARC-4 → ARC-5 → ARC-6 → N-1 → N-2 → N-3 → N-4) with a hard rule that D-5.12f cannot start until ARC-4/5/6 are closed. Path A widening applied: `PROTECTED-FILES.md` (3 places), `APPROVAL-GATES.md` (2 places), and `PHASE-MODES.md` (2 places) register `NEXT-ACTION-SELECTOR.md` as a safety-policy doc. Codex docs-only review round 1 = PASS WITH REQUIRED EDITS (4 wording fixes including PHASE-MODES.md registration and rule-6 gate-mapping correction); round 2 = PASS WITH REQUIRED EDITS (7 status-doc wording fixes for Path A seven-file scope); round 3 (post-fixes) = PASS. Operator approved. Committed `afd5930` (7 files, +133 / -23); no runtime / protected file touched.
  - [x] **ARC-5 — Role Hierarchy + Prompt Templates (Phase ARC-5 — mode: DOCS-ONLY) — CLOSED, committed `698e0d3` (2026-05-03).** New `orchestrator/ROLE-HIERARCHY.md` (341 lines) defines five named roles (Victor / ChatGPT / Gemini / Claude / Codex) plus future Ruflo / automation governance-only role. Critical trading-runtime separation rule established. Reusable prompt templates for VP / Director / Builder / CRSO included. Codex docs-only review round 1 = PASS WITH REQUIRED EDITS (3 wording fixes around Victor/Codex override safety: explicit, in-session, named, acknowledged, scoped); round 2 (post-fixes) = PASS. Operator approved. Committed `698e0d3` (7 files, +368 / -23); no runtime / protected file touched.
  - [x] **ARC-6 — Automation Permission Rules (Phase ARC-6 — mode: DOCS-ONLY) — CLOSED, committed `9eff3c9` (2026-05-03).** New `orchestrator/AUTOMATION-PERMISSIONS.md` (236 lines) defines the GREEN / YELLOW / RED three-tier permission model for Claude / Codex / ChatGPT / Gemini / Ruflo / Hermes / future automation. Blocked-commands list, blocked-files list, stop conditions, future-automation governance-only inheritance, and trading-runtime separation reinforced. Codex docs-only review round 1 = PASS (no required edits). Operator approved. Committed `9eff3c9` (7 files, +263 / -25); no runtime / protected file touched.
  - [x] **N-1 — Repo Confirmation (mode: READ-ONLY AUDIT) — PASS (2026-05-03).** HEAD on `9eff3c9`; only untracked `position.json.snap.20260502T020154Z`; no protected file modifications; ARC track 1–6 fully landed in chronological order; all eight safety-policy docs present and mutually cross-referenced; all eight migration files present on disk; Migration 008 file content verified as the D-5.12c `emergency_audit_log` schema. Surfaced one informational finding (stale ARC-6-in-progress wording across STATUS.md / CHECKLIST.md / NEXT-ACTION.md) — addressed by the N-1 closeout sync below. No edits, no commits, no migrations applied, no deploys, no live Kraken actions, no production-DB queries during N-1.
  - [x] **N-1 closeout sync (mode: DOCS-ONLY) — CLOSED, committed `4d472e2` (2026-05-03).** Marked ARC-6 closed (`9eff3c9`) and N-1 PASS across STATUS.md / CHECKLIST.md / NEXT-ACTION.md; removed stale ARC-6-in-progress wording; set next-action pointer to N-2 (since superseded by N-1.5 + ARC-7 master-order change). Codex docs-only review round 1 = PASS WITH REQUIRED EDITS (1 wording fix in STATUS.md `AUTOPILOT-RULES.md`-class peer wording); round 2 (post-fix) = PASS. Operator approved. Committed `4d472e2` (3 files, +21 / -20); no runtime / safety-policy / protected file touched.
  - [x] **N-1.5 — 3-Brains Handoff Packet System Design (mode: DESIGN-ONLY) — PASS (Codex), 2026-05-03.** Design report produced in conversation only (no working-tree files). Proposed handoff packet system: ten Markdown templates under a new `orchestrator/handoffs/` folder, governed by a new safety-policy doc `orchestrator/HANDOFF-RULES.md`. Codex design review round 1 = PASS with non-blocking refinement notes (rename `CURRENT-PHASE.md` → `PHASE-SNAPSHOT.md`, add explicit STOP rule for forbidden content, future-automation packet-write prohibitions, packet-not-canonical reminders). Operator decision: approve master-order insertion of ARC-7 between N-1 and N-2 (RED-tier master-order change). No edits, no commits, no migrations, no deploys, no production-DB queries, no live Kraken actions during N-1.5.
  - [x] **ARC-7 — Handoff Packet Templates (Phase ARC-7 — mode: DOCS-ONLY) — CLOSED, committed `8266db2` (2026-05-03).** New `orchestrator/HANDOFF-RULES.md` (138 lines) plus ten packet templates under `orchestrator/handoffs/`. Codex docs-only review round 1 = PASS (no required edits). Operator approved. Committed `8266db2` (18 files, +622 / -30); no runtime / protected file touched.
  - [x] **N-2 — Migration 008 production-application planning (Phase N-2 — mode: DESIGN-ONLY) — Codex PASS WITH REQUIRED EDITS (2026-05-03).** Conversation-only design report covering Migration 008 file analysis, runner analysis, N-3 application plan, verification, rollback, safety gates, risk table, Codex review prompt, and future-N-3 operator-approval packet draft. Codex returned PASS WITH REQUIRED EDITS with 5 wording tightenings: (1) pre-flight expanded with branch / HEAD / untracked-files / dirty-tree checks; (2) operator-approval wording stabilized; (3) verification expanded to six indexes plus full `schema_migrations` sequence plus 13-column check; (4) emergency stop extended with `applying`-log-line check; (5) risk table extended with two new P0 risks (untracked/dirty 009+; wrong branch/HEAD) plus two new P1 risks (mistaking Codex PASS / clean tree as approval; approval-wording-leaking-secrets). No file edits, no commits, no migrations applied, no deploys, no production-DB queries, no live Kraken actions during N-2.
  - [x] **N-2b — Migration 008 production plan runbook (Phase N-2b — mode: DOCS-ONLY) — CLOSED, committed `e6c9189` (2026-05-03).** New runbook artifact `orchestrator/handoffs/N-2-MIGRATION-008-PRODUCTION-PLAN.md` (14 sections, 185 lines post-cleanup). All 5 Codex required N-2 tightenings applied verbatim; 3 follow-up cleanup rounds achieved full HANDOFF-RULES.md prose-only compliance (zero `railway run` / `DATABASE_URL=` / `node scripts/run-migrations.js` matches). Codex round-3 PASS. Operator approved. Committed `e6c9189` (4 files, +210 / -20).
  - [x] **N-3 preflight review (Codex on committed runbook) — PASS WITH REQUIRED EDITS (2026-05-03).** Codex caught that the committed N-2b runbook still pinned pre-flight check (i) HEAD to `8266db2…` (the pre-N-2b commit), while the runbook is committed at `e6c9189` — pre-flight (i) would fail immediately if N-3 were executed against the runbook as written. Naive fix (update pinned hash to `e6c9189…`) would re-create the same staleness on the next commit (self-staling commit loop). Codex returned PASS WITH REQUIRED EDITS with four wording fixes: (1) status-line update, (2) pre-flight (i) replacement, (3) approval-wording HEAD-naming addition, (4) risk-table reformulation. Operator chose to apply a safer variant: replace the in-runbook hardcoded HEAD pin with an approval-time exact-HEAD-naming rule (avoids the self-staling loop entirely). N-3 remains hard-blocked.
  - [~] **N-2c — Migration 008 runbook HEAD-preflight correction (Phase N-2c — mode: DOCS-ONLY) — IN PROGRESS.**
    - [x] Edit 1 applied: runbook §1 status line; Edit 2: pre-flight (i) approval-time HEAD-naming; Edit 3: §5 exact-HEAD-naming requirement; Edit 4: risk-table P0 wrong-HEAD/branch row; Edit 5: change-history N-2c entry. Codex docs-only review round-2 PASS. Operator approved. Committed at `9ae139d`.
  - [x] **N-3 preflight review (Codex on committed N-2c runbook) — PASS (2026-05-03).** Codex confirmed the N-2c runbook is N-3-ready with the approval-time exact-HEAD-naming rule fully resolving the stale-pinned-HEAD finding. No further runbook iteration needed at that point. N-3 was waiting solely on Victor's in-session approval naming exact current HEAD + execution-time pre-flight pass + target confirmation.
  - [x] **Victor N-3 production-action approval — granted 2026-05-03.** Approval named exact HEAD `9ae139d462e6cae72cd3a086cafaa2423ebe8799`, scoped to single-action Migration 008 application + verification, expires when action ends, explicitly excluded all other production actions.
  - [x] **N-3 attempt — HALTED (2026-05-03).** Pre-flight (i)-(v) PASS (Claude-runnable git checks); pre-flight (vi) operator-confirmed Yes. Operator-driven runner invocation HALTED with **runner exit 1**, final log line reporting `[migrate] fatal: getaddrinfo ENOTFOUND` for the internal hostname. **No `applying` log line appeared. No `BEGIN` issued. No SQL executed. Production-DB state unchanged. `schema_migrations` not bootstrapped. `emergency_audit_log` not created. No partial state. No rollback needed and none authorized.** Prior Victor approval consumed by failed attempt; cannot be reused. Captured runner output preserved as a local timestamped file on operator's machine. Root cause: the Railway-internal Postgres hostname does not resolve from outside Railway's network. The N-2c runbook did not document the Railway-attached execution-context requirement explicitly.
  - [x] **N-2d — Migration 008 Railway-internal connectivity runbook update (Phase N-2d — mode: DOCS-ONLY) — CLOSED, committed `3732721` (2026-05-03).**
    - [x] Added §1.1 N-3 attempt history documenting runner exit 1, hostname-resolution failure, no SQL executed, production-DB unchanged, prior approval consumed, captured output preserved locally, root-cause diagnosis.
    - [x] Extended §3 Migration runner behavior with Railway-attached execution-context requirement (prose only — no literal commands, hostnames, ports, or URLs).
    - [x] Added pre-flight check (vii): operator must confirm reachability of the production Postgres target from the chosen execution context before invoking the runner. Operator-driven, prose-only confirmation. HALT on unreachable target.
    - [x] Extended §7 Emergency stop conditions with: HALT on unreachable target before runner invocation; HALT on connection-failure runner exits (1 with hostname-resolution / connection-refused / TLS-handshake errors, or 2 for missing config); explicit "do not retry automatically — every retry requires fresh Codex preflight PASS + fresh Victor approval naming exact current HEAD".
    - [x] Extended §8 Risk table with two new rows: P0 (local execution context cannot resolve Railway-internal Postgres hostname; mitigation: pre-flight (vii) connectivity preflight, preferred Railway-attached execution context); P1 (operator retries with consumed approval; mitigation: §11 N-3 gate re-satisfied from scratch on every retry).
    - [x] Added §14 change-history entries for the N-3 attempt and the N-2d update.
    - [x] Runbook remains fully prose-only and HANDOFF-RULES.md compliant — zero literal Railway runner commands, zero `DATABASE_URL` examples, zero hostname / port / URL values added.
    - [x] N-2d status entries added to `orchestrator/STATUS.md`, `orchestrator/CHECKLIST.md`, `orchestrator/NEXT-ACTION.md`.
    - [x] No edits to safety-policy docs (`PROTECTED-FILES.md`, `APPROVAL-GATES.md`, `PHASE-MODES.md`, `NEXT-ACTION-SELECTOR.md`, `ROLE-HIERARCHY.md`, `AUTOMATION-PERMISSIONS.md`, `AUTOPILOT-RULES.md`, `BLUEPRINT.md`, `HANDOFF-RULES.md`).
    - [x] No edits to the ten ARC-7 packet templates outside the runbook artifact.
    - [x] No edits to `bot.js`, `dashboard.js`, `db.js`, `migrations/`, `scripts/`, or `position.json`.
    - [x] No migrations applied. No deploys. No production-DB queries. No Railway commands. No live Kraken actions. No env / secret reads or writes. No `MANUAL_LIVE_ARMED` change. No automation install.
    - [x] Codex docs-only review round 1 = PASS WITH REQUIRED EDITS (9 wording fixes); round 2 = FAIL WITH REQUIRED EDITS (residual literal hostname leak in CHECKLIST.md:235); round 3 = PASS.
    - [x] Operator approval to commit N-2d granted 2026-05-03.
    - [x] Committed `3732721` exactly the runbook + three status docs (4 files, +58 / -24).
  - [x] **N-3 preflight review (Codex on committed N-2d runbook, HEAD `3732721`) — PASS WITH REQUIRED EDITS (2026-05-03).** Codex flagged eight surfaces: (a) §4(vii) under-specified categories — must distinguish DNS / TLS-SSL / auth / target-identity; (b) §5 approval block missing HEAD placeholder; (c) §6 verification limited to names not definitions — must check types / NOT NULL / defaults / PK / UNIQUE / CHECK / partial predicates / DESC sort order; (d) §7 conflating runner exit 1 with exit 2; (e) §7 missing HALT-on-no-applying-008-line and HALT-on-all-migrations-applied rules; (f) §8 missing concurrent-runner / dotenv-shadow / stale-Railway-CLI / runner-version-drift rows; (g) §9 stale "any future N-2b commit-time approval" wording; (h) §14 stale "Pending commit" status on N-2d entry. N-3 remains hard-blocked.
  - [x] **N-2e — Migration 008 N-3 preflight tightening (Phase N-2e — mode: DOCS-ONLY) — CLOSED, committed `afe94d1` (2026-05-03).**
    - [x] Edit 1 applied: §4(vii) connectivity preflight tightened from prose-only "reachable" to four PASS/FAIL categories — (a) DNS / name resolution; (b) TLS / SSL negotiation or explicitly-not-applicable; (c) Postgres authentication; (d) authenticated-target identity confirmed via non-secret service / add-on identity surface or approved read-only identity check. Operator records only PASS/FAIL category labels — never hostname, port, URL, username, password, `DATABASE_URL` value, or query result. On failure, classify as DNS / TLS-SSL / auth / wrong-target-or-unknown-target and HALT before runner invocation.
    - [x] Edit 2 applied: §5 approval-wording block extended with explicit `for exact HEAD EXACT_CURRENT_HEAD_HASH_FROM_GIT_REV_PARSE_HEAD only` placeholder so the highest-salience approval text mirrors the §5 exact-HEAD-naming requirement.
    - [x] Edit 3 applied: §6 verification expanded — (d) full column-definition check (column names, types, NOT NULL flags, defaults, PK on `id`, `event_id` UNIQUE, `mode IN ('paper','live')` CHECK constraint via `information_schema.columns`, `pg_constraint`, `pg_index`); (e) full index-definition check (six index names, indexed columns and sort order including DESC where present, uniqueness / PK origin, partial predicates for `unresolved` and `kraken_order_id` indexes via `pg_indexes` + `pg_index`).
    - [x] Edit 4 applied: §7 exit-code stop condition disambiguated — exit 1 = fatal connection / protocol; exit 2 = missing `DATABASE_URL` / missing migrations directory; do not conflate.
    - [x] Edit 5 applied: §7 unexpected runner-output stop condition extended — HALT on no `applying 008_emergency_audit_log.sql` log line on exit 0; HALT on `all migrations applied` no-op output unless §6 (a)–(e) verifies same-invocation application.
    - [x] Edit 6 applied: §8 risk table extended with four new rows — P1 concurrent runner from second shell / deploy step / Railway lifecycle hook (mitigation tied to new §4(viii)); P1 `DATABASE_URL` / env shadowed by local `.env` (mitigation tied to new §4(ix)); P1 stale Railway CLI / project / service auth context (mitigation tied to §4(vii) category (d)); P2 runner version drift between approved HEAD and Railway-attached execution (mitigation tied to §4(i) + (iii) plus operator-confirmed Railway-attached runtime matches approved HEAD).
    - [x] Edit 7 applied: §9 gate-separation note updated from "any future N-2b commit-time approval" to "any N-2 / N-2b / N-2c / N-2d / N-2e commit-time approval will NOT authorize gate 4".
    - [x] Edit 8 applied: §14 N-2d entry stale-state correction — "Pending Codex docs-only review and explicit operator approval before commit" → "Committed at HEAD `3732721`. Descriptive history only. This entry does not authorize N-3; N-3 remains blocked behind §11."
    - [x] New §4(viii) concurrent-runner confirmation pre-flight check added: operator confirms no other process / shell / deploy step / Railway lifecycle hook is configured to invoke the migration runner against the same target during the N-3 window. HALT until paused or N-3 window rescheduled.
    - [x] New §4(ix) no-local-`.env`-shadow pre-flight check added: operator confirms no `.env` file is present in the execution context's working directory. HALT until removed, renamed, or operator switches working directory.
    - [x] §11 N-3 gate condition (4) updated from "(i)–(vii) pass" to "(i)–(ix) pass".
    - [x] Pre-flight expands from seven checks to nine across runbook + STATUS.md + CHECKLIST.md + NEXT-ACTION.md derivative references.
    - [x] §14 N-2e change-history entry added.
    - [x] Runbook remains fully prose-only and HANDOFF-RULES.md compliant — zero literal Railway runner commands, zero `DATABASE_URL` values, zero hostname / port / URL values added.
    - [x] N-2e status entries added to `orchestrator/STATUS.md`, `orchestrator/CHECKLIST.md`, `orchestrator/NEXT-ACTION.md`.
    - [x] No edits to safety-policy docs (`PROTECTED-FILES.md`, `APPROVAL-GATES.md`, `PHASE-MODES.md`, `NEXT-ACTION-SELECTOR.md`, `ROLE-HIERARCHY.md`, `AUTOMATION-PERMISSIONS.md`, `AUTOPILOT-RULES.md`, `BLUEPRINT.md`, `HANDOFF-RULES.md`).
    - [x] No edits to the ten ARC-7 packet templates outside the runbook artifact.
    - [x] No edits to `bot.js`, `dashboard.js`, `db.js`, `migrations/`, `scripts/`, or `position.json`.
    - [x] No migrations applied. No deploys. No production-DB queries. No Railway commands. No live Kraken actions. No env / secret reads or writes. No `MANUAL_LIVE_ARMED` change. No automation install.
    - [x] Codex docs-only review of N-2e runbook diff + status-doc edits = PASS (closure round, after 3 stale-label corrections).
    - [x] Operator approval to commit N-2e granted 2026-05-03.
    - [x] Committed `afe94d1` exactly the runbook + three status docs (4 files, +53 / -22).
  - [x] **N-3 preflight review (Codex on committed N-2e runbook, HEAD `afe94d1`) — PASS WITH REQUIRED EDITS (2026-05-03).** Codex flagged eight surfaces: (a) §2 missing explicit forward-only rollback blocker — the runbook said rollback is destructive and not pre-authorized but did not block post-failure operator pressure to "clean up" via DROP TABLE / compensating DDL; (b) §4 missing runtime-source identity check — the runner SOURCE FILE was constrained to the approved HEAD by (i)+(iii), but the Railway-attached runtime resolving the runner / migration / Node / `node_modules` from the approved HEAD/runtime was buried in a risk row, not a §4 check; (c) §4 missing pre-apply DB baseline check — `CREATE TABLE IF NOT EXISTS` could no-op against a pre-existing malformed table, and the runner records version 8 only after running the body, so a pre-existing wrong table could be detected only after version 8 is inserted; (d) §7 missing HALT-on-missing-or-truncated-capture and HALT-on-COMMIT-path-uncertainty rules — §7 depends on captured `applying` lines but did not HALT on capture problems, and the connection-loss-after-COMMIT case was wrongly framed as "partial state cannot exist"; (e) §8 P0 network-partition row's "partial state cannot exist" framing wrong for COMMIT-path uncertainty — the client-observed outcome is unknown, not none; (f) §8 P0 concurrent-runner row's advisory-lock claim factually wrong — the runner does an unlocked SELECT before INSERT, and prevention is procedural; (g) §8 runner-version-drift mis-prioritized at P2 — should be P1 with mitigation tied to a §4 check, not buried in (i)+(iii); (h) §8 missing pre-existing-baseline row tied to a new §4 check. N-3 remains hard-blocked.
  - [x] **N-2f — Migration 008 N-3 preflight DDL-baseline + rollback-blocker tightening (Phase N-2f — mode: DOCS-ONLY) — CLOSED, committed `3af1e44` (2026-05-03).**
    - [x] Edit 1 applied: §2 explicit forward-only rollback blocker — if any N-3 failure occurs after runner invocation, rollback / `DROP TABLE emergency_audit_log` / compensating DDL / any production schema mutation is prohibited unless a separate Codex-reviewed rollback runbook + fresh Victor approval. Applies regardless of failure class (connection, exit-1/2/3/4, COMMIT-path uncertainty, partial verification mismatch). Operator pressure to "clean up" by dropping the table explicitly disallowed.
    - [x] Edit 2 applied: §4 new pre-flight check (x) runtime-source identity confirmation — operator confirms working tree or Railway-attached runtime executing the runner is the approved HEAD; if Railway-attached, operator confirms (without exposing secrets) that the runtime/deployed revision resolves runner / migration / Node / `node_modules` from the approved HEAD/runtime via Railway dashboard deployed-revision identity, non-secret build-output marker, or approved read-only check. HALT if cannot be confirmed.
    - [x] Edit 3 applied: §4 new pre-flight check (xi) pre-apply production DB baseline confirmation — operator performs read-only target-state check, recording PASS/FAIL labels only (never query result values, never schema-row content): (a) `schema_migrations` exists; (b) contains exactly versions 1–7; (c) version 8 absent; (d) no version > 8; (e) `emergency_audit_log` does not exist as table or view. HALT before runner invocation if any baseline condition fails or cannot be verified. Guards against `CREATE TABLE IF NOT EXISTS` no-op against pre-existing malformed table, missing/mis-bootstrapped `schema_migrations`, unexpected version drift.
    - [x] Edit 4 applied: §4 closing HALT sentence updated from `(i)–(ix)` to `(i)–(xi)`.
    - [x] Edit 5 applied: §7 HALT rules extended — HALT if captured runner output is missing / incomplete / truncated / cannot be reviewed well enough to classify applying lines and final exit state; HALT if connection or protocol failure occurs after applying-008 line appears or during/after COMMIT (classify database outcome as unknown — not "succeeded" and not "rolled back" — do not retry, do not rollback, do not invoke DROP TABLE / compensating DDL, perform only read-only §6 (a)–(e) target-state verification before any further action; committed-or-not outcome is determined by §6 verification alone).
    - [x] Edit 6 applied: §8 risk table corrected — P0 network-partition row reframed to "Network partition or connection loss during/after COMMIT leaves the operator unable to know whether Migration 008 committed" with outcome-unknown semantics tied to §6 verification; P0 concurrent-runner row corrected to remove the false advisory-lock claim and clarify procedural-only prevention (the runner does an unlocked SELECT before INSERT and concurrent invocations can race); P2 runner-version-drift promoted to P1 and mitigation tied to new pre-flight (x); new P1 row added for pre-existing `emergency_audit_log` / unexpected `schema_migrations` baseline tied to new pre-flight (xi).
    - [x] Edit 7 applied: §11 N-3 gate condition (1) updated from "Codex returns PASS on this N-2d docs-only runbook update" to "Codex returns PASS on this N-2f docs-only runbook update" referencing `orchestrator/handoffs/CODEX-VERDICT.md`; condition (4) updated from `(i)–(ix)` to `(i)–(xi)` listing the two new checks (x) runtime-source identity and (xi) pre-apply production DB baseline.
    - [x] Edit 8 applied: §14 N-2e entry stale-state correction — "Pending Codex docs-only review and explicit operator approval before commit" → "Committed at HEAD `afe94d1`. Descriptive history only. This entry does not authorize N-3; N-3 remains blocked behind §11."
    - [x] Pre-flight expands from nine checks to eleven across runbook + STATUS.md + CHECKLIST.md + NEXT-ACTION.md derivative references.
    - [x] §14 N-2f change-history entry added describing all 8 Codex-required edits + new (x)/(xi) checks.
    - [x] Runbook remains fully prose-only and HANDOFF-RULES.md compliant — zero literal Railway runner commands, zero `DATABASE_URL` values, zero hostname / port / URL values added.
    - [x] N-2f status entries added to `orchestrator/STATUS.md`, `orchestrator/CHECKLIST.md`, `orchestrator/NEXT-ACTION.md`.
    - [x] No edits to safety-policy docs (`PROTECTED-FILES.md`, `APPROVAL-GATES.md`, `PHASE-MODES.md`, `NEXT-ACTION-SELECTOR.md`, `ROLE-HIERARCHY.md`, `AUTOMATION-PERMISSIONS.md`, `AUTOPILOT-RULES.md`, `BLUEPRINT.md`, `HANDOFF-RULES.md`).
    - [x] No edits to the ten ARC-7 packet templates outside the runbook artifact.
    - [x] No edits to `bot.js`, `dashboard.js`, `db.js`, `migrations/`, `scripts/`, or `position.json`.
    - [x] No migrations applied. No deploys. No production-DB queries. No Railway commands. No live Kraken actions. No env / secret reads or writes. No `MANUAL_LIVE_ARMED` change. No automation install.
    - [x] Codex docs-only review of N-2f runbook diff + status-doc edits = PASS (closure round, after 4 stale-wording corrections).
    - [x] Operator approval to commit N-2f granted 2026-05-03.
    - [x] Committed `3af1e44` exactly the runbook + three status docs (4 files, +50 / -25).
  - [x] **N-3 preflight review (fresh Codex on committed N-2f runbook, HEAD `3af1e44`) — FAIL (2026-05-03).** Codex flagged four surfaces requiring further tightening: (a) §4(x) under-specified — runtime-source identity check gave only permissive examples ("Railway dashboard deployed-revision identity", "non-secret build-output marker", "approved read-only check") and would let a near-HEAD / short-hash / branch-name-only / service-name-only / "latest deploy" identity pass; an exact full-hash comparison against Victor's named HEAD was not required; (b) §4(xi) under-specified — pre-apply DB baseline check did not name the exact query target (`schema_migrations.version`), did not constrain timing relative to the §4(viii) concurrent-runner confirmation (race window between baseline read and runner invocation), and did not link runner no-op / all-migrations-applied output to the §7 HALT path; (c) §8 mitigation chains inherited the under-specified (x) and (xi) surfaces, plus the wrong-target row cited only §4(vi) and not §4(vii)(d) (gap); (d) §14 / phase label / status chain stale — runbook header still read "Phase: N-2b" (original from N-2b commit, never updated through N-2c → N-2f), §1 status chain stopped at `afe94d1` (N-2e), §14 N-2f tail still said "Pending Codex docs-only review and explicit operator approval before commit" even though N-2f committed at `3af1e44`. N-3 remains hard-blocked.
  - [x] **N-2g — Migration 008 N-3 preflight runtime-identity + DB-baseline + descriptive-state tightening (Phase N-2g — mode: DOCS-ONLY) — CLOSED, committed `926eb7f` (2026-05-03).**
    - [x] Edit 1 applied: §4(x) tightened to require an exact full-hash comparison between the execution context's source identity and the full HEAD hash named in Victor's fresh N-3 approval. Procedure by execution-context type: (a) local execution = `git rev-parse HEAD` from the execution checkout; (b) Railway-attached execution = deployed-revision / source-identity surface for the attached service. Near-HEAD / short-hash / branch-name / service-name / "latest deploy" identity is explicitly NOT sufficient. The operator additionally confirms that runner / migration / Node / `node_modules` resolve from that exact-hash source. HALT if cannot be performed without exposing secrets, or if comparison does not match exactly.
    - [x] Edit 2 applied: §4(xi) tightened — names the exact query target `schema_migrations.version`; requires the baseline check to immediately precede runner invocation after the §4(viii) concurrent-runner confirmation; the `schema_migrations.version` column must return exactly `{1, 2, 3, 4, 5, 6, 7}` (no missing / duplicate / extra), value `8` absent, no value > `8`, `emergency_audit_log` does not exist verified via `information_schema.tables` / `information_schema.views` or approved equivalent. Timing constraint: must run last among (i)–(xi). If §4(viii) cannot be re-asserted at the moment of (xi), HALT. No-op runner output linkage: if the runner is invoked after (xi) PASS but reports `all migrations applied` / no-op / no `applying 008_emergency_audit_log.sql` line, that is handled under §7 HALT — do NOT re-run the runner.
    - [x] Edit 3 applied: §8 risk-table mitigation chain updates — runner-version-drift mitigation rewritten to cite the tightened §4(x) full-hash comparison (explicit byte-for-byte 40-character comparison; near-HEAD / short-hash / branch / service / "latest deploy" NOT sufficient); pre-existing-baseline mitigation rewritten to cite the tightened §4(xi) (immediate-before-invocation timing after §4(viii); `schema_migrations.version` query target; `{1..7}` exact set check; no-op runner output linkage to §7 HALT — do NOT re-run); wrong-target mitigation extended to cite both §4(vi) AND §4(vii)(d) (both must PASS independently).
    - [x] Edit 4 applied: runbook header phase label corrected from "Phase: N-2b — DOCS-ONLY runbook artifact" to "Phase: N-2g — DOCS-ONLY runbook artifact (current; see §14 for full chain)"; §1 status chain extended to include `3af1e44` (N-2f); §14 N-2f entry tail rewritten from "Pending Codex docs-only review and explicit operator approval before commit" to "Committed at HEAD `3af1e44daf736b825f9f352a66304bce28755e85`. Descriptive history only. This entry does not authorize N-3; N-3 remains blocked behind §11."
    - [x] Pre-flight remains at eleven checks (i)–(xi); no new check added in N-2g.
    - [x] §14 N-2g change-history entry added describing all 4 Codex-required edits.
    - [x] Runbook remains fully prose-only and HANDOFF-RULES.md compliant — zero literal Railway runner commands, zero `DATABASE_URL` values, zero hostname / port / URL values added.
    - [x] N-2g status entries added to `orchestrator/STATUS.md`, `orchestrator/CHECKLIST.md`, `orchestrator/NEXT-ACTION.md`.
    - [x] No edits to safety-policy docs (`PROTECTED-FILES.md`, `APPROVAL-GATES.md`, `PHASE-MODES.md`, `NEXT-ACTION-SELECTOR.md`, `ROLE-HIERARCHY.md`, `AUTOMATION-PERMISSIONS.md`, `AUTOPILOT-RULES.md`, `BLUEPRINT.md`, `HANDOFF-RULES.md`).
    - [x] No edits to the ten ARC-7 packet templates outside the runbook artifact.
    - [x] No edits to `bot.js`, `dashboard.js`, `db.js`, `migrations/`, `scripts/`, or `position.json`.
    - [x] No migrations applied. No deploys. No production-DB queries. No Railway commands. No live Kraken actions. No env / secret reads or writes. No `MANUAL_LIVE_ARMED` change. No automation install.
    - [x] Codex docs-only review of N-2g runbook diff + status-doc edits = PASS (closure round, after 4 stale-reference corrections).
    - [x] Operator approval to commit N-2g granted 2026-05-03.
    - [x] Committed `926eb7f` exactly the runbook + three status docs (4 files, +42 / -24).
  - [x] **N-3 preflight review (fresh Codex on committed N-2g runbook, HEAD `926eb7f`) — PASS WITH REQUIRED EDITS (2026-05-03).** Single finding: §14 N-2g entry tail still said "Pending Codex docs-only review and explicit operator approval before commit" even though N-2g had committed. Codex itself recognized this as a recurring stale-tail loop pattern: every N-2x commit creates a stale §14 tail in its own change-history entry that the next preflight then flags, requiring an N-2(x+1) commit to close, which itself drafts a new stale tail (N-2d → N-2e → N-2f → N-2g all hit this). All other 14 substantive checks PASSed (§4(x) full-hash, §4(xi) DB baseline, §7 COMMIT-unknown HALT, §8 risk mitigations, §11 five-condition gate, §6 post-apply verification). N-3 remains hard-blocked.
  - [x] **N-2h — Migration 008 structural §14 stale-tail loop fix (Phase N-2h — mode: DOCS-ONLY) — CLOSED, committed `ea7774d` (2026-05-03).**
    - [x] Edit 1 applied: §14 preamble added — "§14 is descriptive history only. It records what each phase changed, not the current commit state or authorization state. The top-of-file Status block (§1, 'Status:' line) is the canonical source for HEAD-of-record, including the latest committed runbook revision. §11 is the canonical source for N-3 authorization state (the five-condition gate). Per-entry 'Pending commit' / 'Committed at HEAD' / 'This entry does not authorize N-3' tails were removed in N-2h to break a recurring stale-tail loop; those facts now live in §1 (HEAD-of-record) and §11 (gate) where they auto-stay-current with the actual repository state and gate logic."
    - [x] Edit 2 applied: stale-prone tails stripped from N-2b → N-2g §14 entries — "Committed at `e6c9189`." (N-2b), "Committed at `9ae139d`." (N-2c), "Committed at HEAD `3732721`. Descriptive history only. This entry does not authorize N-3; N-3 remains blocked behind §11." (N-2d), same pattern (N-2e), same pattern (N-2f), and "N-3 remains blocked pending Codex docs-only PASS on this N-2g update… Pending Codex docs-only review and explicit operator approval before commit." (N-2g) all removed. What each phase changed and why is preserved.
    - [x] Edit 3 applied: new §14 N-2h entry added describing the structural loop fix and noting that no §4 / §5 / §6 / §7 / §8 / §11 substance changed.
    - [x] Edit 4 applied: §1 Status block updated — phase label "N-2g" → "N-2h"; status chain extended to include `926eb7f` (N-2g); N-2h description added.
    - [x] Edit 5 applied: §11 condition (1) updated from "Codex returns PASS on this N-2g docs-only runbook update" to "Codex returns PASS on this N-2h docs-only runbook update"; binding-state wording updated from N-2g artifact to N-2h artifact.
    - [x] Edit 6 applied: §9 gate-separation wording extended from "any N-2 / N-2b / N-2c / N-2d / N-2e / N-2f commit-time approval" to "any N-2 / N-2b / N-2c / N-2d / N-2e / N-2f / N-2g / N-2h commit-time approval will NOT authorize gate 4".
    - [x] Pre-flight remains at eleven checks (i)–(xi); no new check added in N-2h.
    - [x] No §4 / §5 / §6 / §7 / §8 / §11 substance changed — N-2h is purely a history-doc structural cleanup plus the active-gate label updates.
    - [x] Runbook remains fully prose-only and HANDOFF-RULES.md compliant — zero literal Railway runner commands, zero `DATABASE_URL` values, zero hostname / port / URL values added.
    - [x] N-2h status entries added to `orchestrator/STATUS.md`, `orchestrator/CHECKLIST.md`, `orchestrator/NEXT-ACTION.md`.
    - [x] No edits to safety-policy docs (`PROTECTED-FILES.md`, `APPROVAL-GATES.md`, `PHASE-MODES.md`, `NEXT-ACTION-SELECTOR.md`, `ROLE-HIERARCHY.md`, `AUTOMATION-PERMISSIONS.md`, `AUTOPILOT-RULES.md`, `BLUEPRINT.md`, `HANDOFF-RULES.md`).
    - [x] No edits to the ten ARC-7 packet templates outside the runbook artifact.
    - [x] No edits to `bot.js`, `dashboard.js`, `db.js`, `migrations/`, `scripts/`, or `position.json`.
    - [x] No migrations applied. No deploys. No production-DB queries. No Railway commands. No live Kraken actions. No env / secret reads or writes. No `MANUAL_LIVE_ARMED` change. No automation install.
    - [x] Codex docs-only review of N-2h runbook diff + status-doc edits = PASS (single closure round, no stale-reference fixes needed).
    - [x] Operator approval to commit N-2h granted 2026-05-03.
    - [x] Committed `ea7774d` exactly the runbook + three status docs (4 files, +46 / -24).
  - [x] **N-3 preflight review (fresh Codex on committed N-2h runbook, HEAD `ea7774d`) — FAIL (2026-05-03).** Codex flagged four findings: (a) §1 Status block did not include N-2h's own commit hash because §1 had been drafted pre-commit, mirroring the §14 stale-tail loop the N-2h refactor was supposed to break — the loop simply moved from §14 to §1; (b) §14 preamble's "auto-stay-current" claim was incorrect since §1 must be actively maintained, not self-updating; (c) §8 P1 stale-Railway-CLI row contained literal `railway run` / `railway shell` command-string references, a HANDOFF-RULES.md production-runner-command leak that had been in the runbook since N-2e and was missed by all four prior preflights; (d) §4(x) / §8 runner-version-drift mitigation cited the §4(x) full-hash comparison as the catch for Node/runtime/dependency drift, but `git rev-parse HEAD` only catches source-file identity, not Node binary version or `node_modules` content. N-3 remains hard-blocked.
  - [x] **N-2i — Migration 008 runbook self-hash loop removal + runtime drift tightening (Phase N-2i — mode: DOCS-ONLY) — CLOSED, committed `5ee1dcb` (2026-05-03).**
    - [x] Edit 1 applied: §1 Status block self-hash chain removed — replaced with prose phase chain ("N-2b → N-2c → N-2d → N-2e → N-2f → N-2g → N-2h → N-2i (current)") and explicit deferral of commit-hash truth to `git log` / `git show`. The runbook no longer tries to record its own committed HEAD; future N-2x commits don't have to re-edit §1 just to record their own new HEAD.
    - [x] Edit 2 applied: §14 preamble qualified — "auto-stay-current" claim removed. New preamble explicitly states `git log` / `git show` are the canonical commit-truth source and §11 is the canonical N-3 gate source.
    - [x] Edit 3 applied: §8 P1 stale-Railway-CLI row literal `railway run` / `railway shell` strings replaced with generic prose ("a Railway-attached CLI or shell execution surface"). HANDOFF-RULES.md compliance restored — verified by `grep -n "railway run\|railway shell"` returning no matches.
    - [x] Edit 4 applied: §4(x) tightened from a single full-hash check into a three-component identity check — (a) source-revision full-hash match (preserved verbatim from N-2g), (b) Node-runtime identity match (Node major+minor+patch version equals the approved-HEAD `package.json` "engines.node" pin; HALT if no pin or version cannot be confirmed without exposing secrets), (c) dependency / build identity match (`node_modules` resolves from a lockfile-respecting build of the approved-HEAD `package.json` + lockfile; non-secret deployed-build identity surface for Railway-attached execution). All three (a), (b), (c) must PASS independently; source HEAD match alone is NOT sufficient.
    - [x] Edit 5 applied: §8 runner-version-drift row mitigation rewritten to cite the three-component (x) check — explicitly states "All three (a), (b), (c) must PASS independently; source HEAD match alone is NOT sufficient."
    - [x] §11 condition (1) updated from "Codex returns PASS on this N-2h docs-only runbook update" to "Codex returns PASS on this N-2i docs-only runbook update"; binding-state wording updated from N-2h artifact to N-2i artifact.
    - [x] §9 gate-separation chain extended from "any N-2 / N-2b / N-2c / N-2d / N-2e / N-2f / N-2g / N-2h commit-time approval" to "any N-2 / N-2b / N-2c / N-2d / N-2e / N-2f / N-2g / N-2h / N-2i commit-time approval will NOT authorize gate 4".
    - [x] §14 N-2i entry added describing all four structural fixes; entry deliberately ends without a stale-prone "Pending commit" / "Committed at HEAD" tail, following the precedent set by N-2h.
    - [x] No §4(i)–(ix) / §4(xi) / §5 / §6 / §7 / §8 risk-table-substance other than the runner-version-drift row is changed; §4(x) is strengthened (one full-hash check expanded into three-component identity check), not weakened. Pre-flight remains at eleven checks (i)–(xi); no new check added.
    - [x] Runbook remains fully prose-only and HANDOFF-RULES.md compliant — zero literal Railway runner commands, zero `DATABASE_URL` values, zero hostname / port / URL values added.
    - [x] N-2i status entries added to `orchestrator/STATUS.md`, `orchestrator/CHECKLIST.md`, `orchestrator/NEXT-ACTION.md`.
    - [x] No edits to safety-policy docs (`PROTECTED-FILES.md`, `APPROVAL-GATES.md`, `PHASE-MODES.md`, `NEXT-ACTION-SELECTOR.md`, `ROLE-HIERARCHY.md`, `AUTOMATION-PERMISSIONS.md`, `AUTOPILOT-RULES.md`, `BLUEPRINT.md`, `HANDOFF-RULES.md`).
    - [x] No edits to the ten ARC-7 packet templates outside the runbook artifact.
    - [x] No edits to `bot.js`, `dashboard.js`, `db.js`, `migrations/`, `scripts/`, or `position.json`.
    - [x] No migrations applied. No deploys. No production-DB queries. No Railway commands. No live Kraken actions. No env / secret reads or writes. No `MANUAL_LIVE_ARMED` change. No automation install.
    - [x] Codex docs-only review of N-2i runbook diff + status-doc edits = PASS (single closure round, all 20 verification checks passed).
    - [x] Operator approval to commit N-2i granted 2026-05-03.
    - [x] Committed `5ee1dcb` exactly the runbook + three status docs (4 files, +44 / -23).
  - [x] **N-3 preflight review (fresh Codex on committed N-2i runbook, HEAD `5ee1dcb`) — FAIL (2026-05-03).** Single substantive finding (F-1): §4(x)(b) Node-runtime identity check assumed `package.json` `"engines.node"` would be an exact `major.minor.patch` pin, but the actual repository state defines it as the non-exact range `">=18.0.0"`. The HALT path covered "no pin exists" but not "pin exists but is a non-exact range" — leaving (b) trivially defeatable since any Node 18+ runtime would match `">=18.0.0"` without catching real version drift. The §1 self-hash loop fix from N-2i held: Codex confirmed PASS on every loop-related check (no self-HEAD SHA in §1 or §14, `git log` / `git show` deferred to, §11 canonical, N-2i labels consistent). N-3 remains hard-blocked.
  - [x] **N-2j — Migration 008 Node runtime exact-version preflight tightening (Phase N-2j — mode: DOCS-ONLY) — CLOSED, committed `548383b` (2026-05-03).**
    - [x] Edit 1 applied: §4(x)(b) tightened to require an exact `major.minor.patch` Node version. **Exact-pin sources, in priority order:** (1) `.nvmrc`; (2) `.node-version`; (3) Volta config in `package.json`; (4) `package.json` `"engines.node"` ONLY IF the value is an exact literal (e.g., `"20.18.5"`); (5) a separately approved non-secret exact-version value supplied by the operator for this single N-3 attempt (recorded only as a non-secret version label, never as a secret). **Non-exact `"engines.node"` semver ranges (containing `>`, `>=`, `^`, `~`, `*`, `||`, `x`, or hyphen ranges) are explicitly NOT an exact pin and do NOT satisfy (b) by themselves.** HALT if no exact pin can be sourced from (1)–(5); HALT if `package.json` `"engines.node"` is the only Node-version source present and is a non-exact range; HALT if execution-context Node version does not match the approved exact-pin value byte-for-byte; HALT if exact runtime Node version cannot be confirmed without exposing secrets.
    - [x] Edit 2 applied: §8 P1 runner-version-drift mitigation rewritten to match the tightened §4(x)(b). Explicit risk-description sentence added: "A non-exact `package.json` `"engines.node"` semver range (e.g., `">=18.0.0"`) does NOT pin a single Node version — any compatible Node major / minor / patch could match, masking real drift." Mitigation cell rewritten to enumerate the (b) priority-ordered exact-pin sources and explicitly reject non-exact ranges.
    - [x] §1 Status block phase label updated from "N-2i" → "N-2j"; phase chain extended to include N-2i (committed) and N-2j (current); description rewritten to summarize the F-1 fix.
    - [x] §11 condition (1) updated from "Codex returns PASS on this N-2i docs-only runbook update" to "Codex returns PASS on this N-2j docs-only runbook update"; binding-state wording updated.
    - [x] §9 gate-separation chain extended from "any N-2 / N-2b / N-2c / N-2d / N-2e / N-2f / N-2g / N-2h / N-2i commit-time approval" to "any N-2 / N-2b / N-2c / N-2d / N-2e / N-2f / N-2g / N-2h / N-2i / N-2j commit-time approval will NOT authorize gate 4".
    - [x] §14 N-2j entry added (no stale-prone "Pending commit" / "Committed at HEAD" tail; follows N-2h/N-2i precedent).
    - [x] No §4(i)–(ix) / §4(x)(a) / §4(x)(c) / §4(xi) / §5 / §6 / §7 substantive logic changed in N-2j; §4(x)(b) is strengthened. Pre-flight remains at eleven checks (i)–(xi); no new check added.
    - [x] Runbook remains fully prose-only and HANDOFF-RULES.md compliant — zero literal Railway runner commands, zero `DATABASE_URL` values, zero hostname / port / URL values added.
    - [x] N-2j status entries added to `orchestrator/STATUS.md`, `orchestrator/CHECKLIST.md`, `orchestrator/NEXT-ACTION.md`.
    - [x] No edits to safety-policy docs (`PROTECTED-FILES.md`, `APPROVAL-GATES.md`, `PHASE-MODES.md`, `NEXT-ACTION-SELECTOR.md`, `ROLE-HIERARCHY.md`, `AUTOMATION-PERMISSIONS.md`, `AUTOPILOT-RULES.md`, `BLUEPRINT.md`, `HANDOFF-RULES.md`).
    - [x] No edits to the ten ARC-7 packet templates outside the runbook artifact.
    - [x] No edits to `bot.js`, `dashboard.js`, `db.js`, `migrations/`, `scripts/`, `position.json`, `package.json`, or any lockfile (the F-1 fix is docs-only — the runbook now defines how the operator handles a non-exact `"engines.node"` range, without requiring the repo state itself to change).
    - [x] No migrations applied. No deploys. No production-DB queries. No Railway commands. No live Kraken actions. No env / secret reads or writes. No `MANUAL_LIVE_ARMED` change. No automation install.
    - [x] Codex docs-only review of N-2j runbook diff + status-doc edits = PASS (single closure round, all 16 verification checks passed).
    - [x] Operator approval to commit N-2j granted 2026-05-03.
    - [x] Committed `548383b` exactly the runbook + three status docs (4 files, +39 / -20).
  - [x] **N-3 preflight review (fresh Codex on committed N-2j runbook, HEAD `548383b`) — FAIL (2026-05-03).** Single substantive finding: §4(x)(b) priority-ordered exact-pin source list did not specify conflict-resolution behavior when multiple sources are present and disagree (e.g., `.nvmrc` says `20.11.0` while Volta config says `22.3.1`) — leaving (b) trivially defeatable since the operator could silently select whichever source happened to match the runtime. The N-2j non-exact-range fix held; the new gap is conflict resolution among multiple exact pins. N-3 remains hard-blocked.
  - [~] **N-2k — Migration 008 Node exact-pin conflict-resolution tightening (Phase N-2k — mode: DOCS-ONLY) — IN PROGRESS.**
    - [x] Edit 1 applied: §4(x)(b) tightened with explicit canonical-source rule and HALT-on-disagreement rule. The highest-priority present exact-pin source (lowest number in the (1)–(5) priority list) is canonical; the runtime must match the canonical source byte-for-byte. If any other present exact-pin source yields a different exact `major.minor.patch` value than the canonical source, HALT — disagreement between exact pins is configuration drift requiring operator investigation, not silent papering-over. The operator must NOT silently choose a lower-priority source over a higher-priority one, must NOT ignore a disagreement, and must NOT pick whichever source happens to match the runtime ("any-source-PASS" is explicitly prohibited). The operator records PASS only if (i) at least one exact-pin source from (1)–(5) yields an exact `major.minor.patch` value, (ii) all other present exact-pin sources from (1)–(5) yield the SAME exact value, and (iii) the execution context's actual Node version matches the canonical value byte-for-byte.
    - [x] Edit 2 applied: §8 P1 runner-version-drift mitigation rewritten to match. Risk description adds: "Multiple exact Node-pin sources in disagreement (e.g., `.nvmrc` says `20.11.0` but Volta config says `22.3.1`) is also a real-drift signal — silently selecting whichever pin matches the runtime would defeat the safety check." Mitigation cell adds: "the highest-priority present exact source is canonical; the runtime must match the canonical source byte-for-byte; if any other present exact-pin source disagrees with the canonical source, HALT (any-source-PASS is explicitly prohibited; the operator must not silently choose a lower-priority source)".
    - [x] §1 Status block phase label updated from "N-2j" → "N-2k"; phase chain extended to include N-2j (committed) and N-2k (current); description rewritten to summarize the conflict-resolution fix.
    - [x] §11 condition (1) updated from "Codex returns PASS on this N-2j docs-only runbook update" to "Codex returns PASS on this N-2k docs-only runbook update"; binding-state wording updated.
    - [x] §9 gate-separation chain extended from "any N-2 / N-2b / N-2c / N-2d / N-2e / N-2f / N-2g / N-2h / N-2i / N-2j commit-time approval" to "any N-2 / N-2b / N-2c / N-2d / N-2e / N-2f / N-2g / N-2h / N-2i / N-2j / N-2k commit-time approval will NOT authorize gate 4".
    - [x] §14 N-2k entry added (no stale-prone "Pending commit" / "Committed at HEAD" tail; follows N-2h/N-2i/N-2j precedent).
    - [x] No §4(i)–(ix) / §4(x)(a) / §4(x)(c) / §4(xi) / §5 / §6 / §7 substantive logic changed in N-2k; §4(x)(b) is strengthened (canonical-source + HALT-on-disagreement rules added). Pre-flight remains at eleven checks (i)–(xi); no new check added.
    - [x] Runbook remains fully prose-only and HANDOFF-RULES.md compliant — zero literal Railway runner commands, zero `DATABASE_URL` values, zero hostname / port / URL values added.
    - [x] N-2k status entries added to `orchestrator/STATUS.md`, `orchestrator/CHECKLIST.md`, `orchestrator/NEXT-ACTION.md`.
    - [x] No edits to safety-policy docs (`PROTECTED-FILES.md`, `APPROVAL-GATES.md`, `PHASE-MODES.md`, `NEXT-ACTION-SELECTOR.md`, `ROLE-HIERARCHY.md`, `AUTOMATION-PERMISSIONS.md`, `AUTOPILOT-RULES.md`, `BLUEPRINT.md`, `HANDOFF-RULES.md`).
    - [x] No edits to the ten ARC-7 packet templates outside the runbook artifact.
    - [x] No edits to `bot.js`, `dashboard.js`, `db.js`, `migrations/`, `scripts/`, `position.json`, `package.json`, or any lockfile (the F-1 fix is docs-only — the runbook now defines how the operator resolves disagreement among multiple exact-pin sources).
    - [x] No migrations applied. No deploys. No production-DB queries. No Railway commands. No live Kraken actions. No env / secret reads or writes. No `MANUAL_LIVE_ARMED` change. No automation install.
    - [ ] Codex docs-only review of the N-2k runbook diff + the three minimal status-doc edits.
    - [ ] Operator approval to commit N-2k.
    - [ ] Commit exactly the runbook + three status docs after Codex PASS + operator approval.
    - [ ] **N-3 (Migration 008 production application, mode PRODUCTION ACTION) remains BLOCKED** until ALL of: (1) Codex docs-only PASS on N-2k; (2) fresh Codex N-3 preflight PASS on the now-updated runbook; (3) fresh Victor in-session production-action approval naming the exact current HEAD at retry time; (4) all eleven pre-flight checks (i)–(xi) PASS at execution time, including the four-category (vii) connectivity preflight, the (viii) concurrent-runner confirmation, the (ix) no-local-`.env`-shadow confirmation, the (x) three-component runtime-source identity confirmation (source revision + exact Node `major.minor.patch` pin (highest-priority present source canonical; HALT on disagreement) + dependency / build identity), and the (xi) pre-apply production DB baseline confirmation; (5) target Railway service / production database confirmed without exposing secrets.
- [~] **Phase D-5.12e closeout docs committed — HOLD for operator-chosen next gated phase.** No coding should happen until the operator chooses the next gated phase. Two natural candidates: **(a) Phase D-5.12f design-only review** for live `SELL_ALL` persistence (mirror of B.1 close-source cleanup for the SELL_ALL surface, with the same DB-first failure-ladder integration D-5.12d/e added) — requires its own design-only Codex review, scoped `dashboard.js` HARD BLOCK lift, Codex implementation review, and explicit operator authorization; **(b) Migration 008 production-application planning** — gated separately; per accepted v2 D-5.12d design, application is a HARD prerequisite before any D-5.12d/e production deployment. The migration runner must NOT be invoked without explicit operator authorization. **No deploy is authorized. No D-5.12f code work is authorized.**

## Full Phase C track — closed

| Sub-phase | Scope | Commit | Closeout |
|---|---|---|---|
| C.1 | `db.js` `loadRecentTradeEvents` admits `manual_sl_update` / `manual_tp_update` | `d0c8817` | `a967a12` |
| C.2 | `dashboard.js` mapper + dedicated "Recent Risk Edits" panel in Performance tab | `2d10107` | `1372392` |
| C.3 | `scripts/recovery-inspect.js` 3-way classification (audit-only — investigate if seen) | `1a16dd8` | `a18b9be` |
| Smoke-test cleanup | `scripts/smoke-test-live-writes.js` four-site wording refresh aligned with post-B.2c bot-side state | `735b10f` | (this closeout) |

Manual SL/TP audit visibility is now complete from DB read (C.1) → UI render (C.2) → operator-playbook classification (C.3) → smoke-test wording aligned with bot-side state (smoke-test cleanup).

## Future phases

- [ ] Phase D-5.12f — Live SELL_ALL persistence as its own sub-phase (close one known bot position only; abort if Kraken balance > qty; LOG_FILE write added to close P0-L2 audit gap)
  - dashboard.js-only. Scoped lift. All-handler smoke required.
  - "Sell all holdings" remains a separately armed future action, not D-5.12f.
- [ ] Phase D-5.12g — Live SET_STOP_LOSS + SET_TAKE_PROFIT persistence (mirror of B.2b-SL / B.2d; B.2c-bot-preserve-TP fix is mode-agnostic and applies to live)
  - dashboard.js-only. Scoped lift. All-handler smoke required.
- [ ] Phase D-5.12h — Live rehydrate enable + recovery scripts + smoke harness + rollback plan
  - May require bot.js scoped lift if `_rehydratePositionJson` mode guard at `bot.js:1322` is widened to live.
  - Update `scripts/reconciliation-shadow.js:228-232` to allow live `--persist`.
  - Update `scripts/recovery-inspect.js` if needed for new emergency_audit_log read.
  - **NEW deliverable:** `scripts/smoke-test-live-dashboard-flow.js` end-to-end harness covering handleTradeCommand flow, MANUAL_LIVE_ARMED gate, transition-lock, emergency-audit/double-fault/triple-fault paths, DB+JSON cache, LOG_FILE/stderr fallbacks, mocked Kraken responses.
  - Carry-forward LOW concerns: Railway stderr retention finite (operator playbook), testcontainer infrastructure (D-5.12h scope), emergency audit latency budget (operator playbook) (Codex v4 notes 9.d / 9.g / 9.h).
- [ ] Phase D-5.12i — Closeout documentation sync (`STATUS.md`, `CHECKLIST.md`, `NEXT-ACTION.md`, `FIX-PLAN.md`, `AUTOPILOT-RULES.md:49`)
  - Document operator playbook for emergency_audit_log triage; document acceptable emergency-audit-insert latency range; document pre-rollback export format for emergency rows; document deploy-warning enforcement-via-CI as future hardening (D-5.13 candidate).
- [ ] Phase D-5.13 (post-D-5.12) — DB-backed `MANUAL_LIVE_ARMED` immediate-disarm + CI deploy-warning enforcement
  - Promote env-var-only MANUAL_LIVE_ARMED to DB-backed for sub-second incident-response disarm (Codex v4 note 9 carryover; operator decision #8 deferred).
  - Add CI/tooling enforcement of the all-handler smoke checklist (Codex v4 note 9.i carryover).
- [ ] Phase O-5 — Bug Audit System
- [ ] Phase O-6 — Security Audit System
- [ ] Phase O-7 — Drift Forensics resumption (Phase 2.5 reactivation; reconciliation persist now schema-unblocked after migration 006 applied)
- [ ] Phase O-8 — Performance & Reliability Upgrades
- [ ] Future reliability item — DB statement timeout via Postgres-side cancellation (`statement_timeout` / `pg_cancel_backend`); not JS-side `Promise.race`
- [ ] Future reliability item — fix latent CLOSE_POSITION live-side stale-source pattern (closed by D-5.12e)
- [ ] Future scaling item — Postgres advisory lock for `/api/control` transition-lock IF Railway dashboard ever scales to multi-replica (Codex v4 note 9 / decision #7 carryover)

## High-risk approval gates

| Gate | Requires | Status |
|---|---|---|
| `db.js` modifications | Operator approval | Closed (post-D-5.12c; lift was scoped) |
| `migrations/` modifications | Operator approval | Closed (post-D-5.12c; lift was scoped) |
| `dashboard.js` modifications | Operator approval | Closed (post-D-5.12e; lift was scoped) |
| `bot.js` modifications | Operator approval | Closed (post-B.2c-bot-preserve-TP; lift was scoped) |
| `scripts/recovery-inspect.js` modifications | Operator approval | Closed (post-C.3; lift was scoped) |
| `scripts/smoke-test-live-writes.js` modifications | Operator approval | Closed (post-smoke-test-cleanup; lift was scoped) |
| Other `scripts/` modifications | Operator approval | Closed (per-file scoped lift required) |
| Live mode write-path changes | Operator approval + Phase D-5.12 | Closed |
| Phase D-5.12a design | Codex 4-iteration design refinement + operator-led safety review + 8 operator decisions accepted | **Closed (v4 PASS WITH NOTES); operator decisions accepted; ready for D-5.12b** |
| Phase D-5.12b implementation (manual live gating) | Scoped `dashboard.js` lift + Codex implementation review + operator authorization | Closed |
| Phase D-5.12c implementation (helpers + migration 008) | Scoped `dashboard.js` + `db.js` + `migrations/` lifts + Codex implementation review + operator authorization | **Closed, committed `4ae3689` (Codex round-2 PASS, all 22 sub-items)** |
| Phase D-5.12c Migration 008 application to production | Separate explicit operator authorization + verify no other unapplied migrations on disk + verify `emergency_audit_log` queryable post-application | **Not given (file present, not applied)** — HARD prerequisite for D-5.12d production deploy per accepted v2 D-5.12d design |
| Phase D-5.12d implementation (live OPEN_LONG / BUY_MARKET persistence) | Scoped `dashboard.js` lift + Codex design review + Codex implementation review + operator authorization | **Closed, committed `1c20177` (Codex round-2 PASS, all 21 sub-items)** |
| Phase D-5.12e implementation (live CLOSE_POSITION persistence) | Scoped `dashboard.js` lift + Codex design review + Codex implementation review + operator authorization | **Closed, committed `95f39f2` (Codex round-2 PASS, all 6 sub-items)** |
| Phase D-5.12d/e production deployment to Railway | Migration 008 applied + verified + explicit operator authorization | **Not given** |
| First production live BUY / live CLOSE exercise | D-5.12d/e deployed + `MANUAL_LIVE_ARMED="true"` + explicit operator authorization | **Not given** |
| Phase D-5.12f through D-5.12g implementation (per-handler live persistence) | Scoped `dashboard.js` lift per sub-phase + all-five-handler smoke per partial deploy + Codex implementation review + operator authorization | **Not authorized / not started** |
| Phase D-5.12h implementation (rehydrate + harness + rollback) | Scoped `bot.js` (potentially) + `scripts/` lifts + Codex implementation review + operator authorization | **Not authorized / not started** |
| Phase D-5.12i closeout docs | orchestrator/* updates only | **Not authorized / not started** |
| Force push / `git reset --hard` / rebase | Explicit operator command | Closed |
| Deployment to Railway | Explicit operator command | Closed |
| Adding new `event_type` values beyond the current 10 | Migration + operator approval | Closed |
| Touching SL / TP / BE / trailing stop logic in bot.js | Explicit operator command | Closed |
| Real-money behavior changes | Operator approval + live mode gate | Closed |
| Reverting migration 006 (drop columns) | Explicit safety review (destructive) | Closed |
