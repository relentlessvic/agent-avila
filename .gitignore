# Credentials — NEVER commit
.env

# Runtime state — local only (NOT committed so Railway redeploys don't reset settings)
safety-check-log.json
position.json
performance-state.json
portfolio-state.json
capital-state.json
bot-control.json
sessions-store.json
.bot.lock
.bot-log-state.json

# Logs & data
*.log
trades.csv

# Dependencies
node_modules/
cookies.txt
