# N-2 Runbook — Migration 008 Production-Application Plan

> **DOCS-ONLY ARTIFACT.** This runbook is a planning document. It does NOT authorize any production action. Per `orchestrator/HANDOFF-RULES.md` and `orchestrator/APPROVAL-GATES.md` gate 4, applying Migration 008 to production is N-3 — a separate PRODUCTION ACTION phase that requires explicit, in-session, scoped Victor / CEO approval that is distinct from any N-2b commit-time approval and from any Codex PASS verdict.
>
> **Canonical authority:** `orchestrator/NEXT-ACTION.md` and `orchestrator/NEXT-ACTION-SELECTOR.md` (per `HANDOFF-RULES.md`). If this runbook ever conflicts with either, the canonical source wins and this runbook is stale.
>
> **No packet substitutes for in-session operator approval** (per `orchestrator/APPROVAL-GATES.md` "What is NOT operator approval"). This runbook describes the shape of an approval; the actual approval is Victor's in-session instruction.

Phase: N-2g — DOCS-ONLY runbook artifact (current; see §14 for full chain)
Last updated: 2026-05-03
Author: Claude (Lead Engineer / Builder)
Status: Committed at `e6c9189` (N-2b) → `9ae139d` (N-2c, HEAD-preflight correction) → `3732721` (N-2d, connectivity-preflight runbook update) → `afe94d1` (N-2e, N-3 preflight tightening) → `3af1e44` (N-2f, N-3 preflight DDL-baseline + rollback-blocker tightening). N-2g is the N-3 preflight runtime-identity + DB-baseline + descriptive-state tightening phase: tightens §4(x) to require an exact full-hash comparison between the execution context's source identity and the full HEAD hash named in Victor's N-3 approval (near-HEAD / short-hash / branch-name / service-name / "latest deploy" identity is not sufficient); tightens §4(xi) to name the exact query target (`schema_migrations.version`), require the baseline check to immediately precede runner invocation after the §4(viii) concurrent-runner confirmation, and link no-op / all-migrations-applied runner output to the §7 HALT path; updates §8 risk-table mitigation chains so runner-version-drift cites the tightened §4(x) full-hash comparison, pre-existing-baseline drift cites the tightened §4(xi) `schema_migrations.version` query and timing, and wrong-target cites both §4(vi) and §4(vii)(d); corrects §14 N-2f entry stale-state from "Pending Codex docs-only review and explicit operator approval before commit" to "Committed at HEAD `3af1e44daf736b825f9f352a66304bce28755e85`. Descriptive history only.". **One N-3 attempt was made and HALTED before any SQL executed** — see §1.1 N-3 attempt history below. **Migration 008 remains NOT applied to production.** N-3 remains blocked pending Codex docs-only PASS on this N-2g update, fresh Codex N-3 preflight PASS on the updated runbook, fresh Victor explicit in-session production-action approval naming the exact current HEAD hash, all eleven pre-flight checks (i)–(xi) passing at execution time, and target Railway service / production database confirmation without exposing secrets.

## 1.1 N-3 attempt history (added in N-2d)

One N-3 attempt was made on 2026-05-03. It HALTED with **runner exit code 1** before any SQL executed. The runner's final log line reported a hostname-resolution failure for the Railway-internal Postgres hostname (`getaddrinfo ENOTFOUND` for the internal hostname). Per `scripts/run-migrations.js:152–154`, exit 1 is a fatal error caught before `pool.connect()` could complete the TLS / Postgres protocol handshake.

What this means concretely:

- **No `applying` log line** appeared in the runner output. The runner did not begin a transaction. No `BEGIN` was issued.
- **No SQL statement was executed** against any database. `schema_migrations` was not bootstrapped. `emergency_audit_log` was not created.
- **Production-DB state is unchanged.** Migration 008 is still NOT applied.
- **No partial state** exists; there is nothing to roll back, and no rollback DDL is authorized.
- **The prior Victor N-3 approval (which named exact HEAD `9ae139d…`) was consumed by this single attempt and cannot be reused.** Per the runbook §5 exact-HEAD-naming requirement, any retry requires a fresh Codex N-3 preflight PASS on the (now-updated) runbook plus a fresh Victor in-session production-action approval naming the exact current HEAD at retry time.
- **Captured runner output** exists as a local timestamped file on the operator's machine. Its contents may include partial environment diagnostics and must not be pasted into any handoff packet (per `HANDOFF-RULES.md` forbidden-content rule).

Root cause (diagnosis): the Railway-internal Postgres hostname resolves only from inside Railway's network (i.e., from a Railway service's runtime or a Railway-attached shell). DNS resolution from the operator's local machine fails immediately at the OS level before the runner can connect. The N-2c runbook did not document this execution-context requirement explicitly, so an operator following the runbook from a non-Railway-attached environment hit this failure mode. N-2d adds the missing connectivity preflight + execution-context guidance to prevent recurrence.

## 1. Phase and scope

- **N-2b — original Migration 008 production plan runbook (DOCS-ONLY).** This phase produced the original stable runbook artifact derived from the conversation-only N-2 design report, with five Codex-required tightening edits applied verbatim. Subsequent N-2c → N-2g phases tightened the artifact further; see §14 change history for the full chain.
- **N-3 remains blocked.** Application of Migration 008 to production is a separate phase that requires Codex PASS on this runbook **and** explicit, in-session, scoped operator approval **and** successful pre-flight checks **and** operator confirmation of the target Railway service / production `DATABASE_URL`.
- **This runbook does NOT authorize production application.** It is planning material only.
- **No migration application, no deploy, no production-DB query, no Railway command, no Kraken action, no env / secret read or write, no `MANUAL_LIVE_ARMED` change, and no runtime edit is authorized by this runbook.**

## 2. Migration 008 summary

- **File:** `migrations/008_emergency_audit_log.sql` (47 lines, committed in D-5.12c `4ae3689`).
- **Classification:** **additive, forward-only, idempotent.**
- **What it creates:**
  - One table: `emergency_audit_log` (13 columns: `id BIGSERIAL PRIMARY KEY`, `event_id TEXT NOT NULL UNIQUE`, `timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `mode TEXT NOT NULL CHECK (mode IN ('paper','live'))`, `source TEXT NOT NULL`, `kraken_order_id TEXT`, `failure_class TEXT NOT NULL`, `error_message TEXT`, `attempted_payload JSONB NOT NULL DEFAULT '{}'::jsonb`, `resolved_at TIMESTAMPTZ`, `resolved_by TEXT`, `resolution_notes TEXT`, `metadata JSONB NOT NULL DEFAULT '{}'::jsonb`).
  - Four explicit indexes (3 partial + 1 full): `emergency_audit_log_unresolved_idx`, `emergency_audit_log_kraken_order_id_idx`, `emergency_audit_log_source_timestamp_idx`, `emergency_audit_log_failure_class_timestamp_idx`.
  - Plus two implicit indexes derived from constraints: PK index on `id`, and the UNIQUE-derived index on `event_id`. **Total: six indexes.**
- **Does NOT touch existing data.** No `ALTER TABLE` on existing tables. No `UPDATE` / `DELETE` / `INSERT`. No reference to `positions`, `trade_events`, `strategy_signals`, or `bot_control`.
- **Does NOT itself contain secrets.** The migration declares schema only.
- **Runtime redaction is a separate contract** in `dashboard.js` (`_redactAttemptedPayload` and `_emergencyAuditWrite`, committed in D-5.12c and exercised in D-5.12d/e). That redaction strips patterns matching `/secret|key|token|cookie|auth|signature|password|credential|nonce/i` from `attempted_payload` before insert at runtime. Not relevant to the migration application itself; relevant only to subsequent live-exercise paths.
- **Reverse path is destructive.** `DROP TABLE emergency_audit_log` would drop historical incident rows. The migration file's header (lines 15–16) explicitly requires "explicit safety review" before any rollback. This runbook does **not** pre-authorize rollback. **Forward-only rollback blocker (added in N-2f):** if any N-3 failure occurs after the runner is invoked, rollback, `DROP TABLE emergency_audit_log`, compensating DDL, or any other production schema mutation is prohibited unless a separate rollback runbook has first passed Codex review and Victor gives fresh, explicit, in-session rollback approval. This blocker applies regardless of failure class — connection failure, exit-1/2 connectivity errors, exit-3 checksum mismatch, exit-4 SQL failure, COMMIT-path uncertainty, or partial verification mismatches all fall under the same prohibition. Operator pressure to "clean up" by dropping the table is explicitly disallowed.

## 3. Migration runner behavior

- **Runner:** `scripts/run-migrations.js` — 154 lines, committed in D-5.4.
- **Invocation forms** (per `package.json:11` and the runner's own usage block at line 14): the runner can be invoked locally through the package migrate script or directly with an explicit database URL. The production N-3 form would use the approved Railway runner invocation. This runbook intentionally omits the literal production command because `HANDOFF-RULES.md` forbids migration-apply commands or production Railway runner invocations inside packet artifacts.
- **Behavior:** reads every `migrations/NNN_*.sql` file matching `/^\d+_.+\.sql$/` (lines 49–51), string-sorts, looks up `schema_migrations` (lines 87–92), and applies every absent version in version order (lines 100–142).
- **Per-migration target?** **No.** The runner has no version-filter flag. **It applies all un-applied migrations sequentially.** This is the same behavior that caused Migration 006 to be applied as a side effect during Phase B.2a.
- **Filesystem-based, not git-tracked.** The runner reads `migrations/*.sql` from the working tree filesystem (lines 49–51), not from `git ls-files`. **Therefore a pre-flight `git ls-files` check alone is insufficient** — the runner would still pick up an untracked or dirty 009+ file. Pre-flight (§4) explicitly guards against this.
- **Safety properties (positive):** transactional per migration (`BEGIN`/`COMMIT`/`ROLLBACK`); checksum-locked (exit 3 on tampered already-applied migration); idempotent (re-runs are no-ops once applied).
- **Execution-context requirement (added in N-2d).** The production Postgres add-on is reachable from inside Railway's network via a Railway-internal hostname; that hostname does not resolve from environments outside Railway's network. The preferred N-3 execution context is therefore a Railway-attached runtime or shell session attached to the Railway project, where the internal Postgres hostname resolves correctly. Any alternative execution context (for example, an operator-local shell using a Railway-published external proxy URL) is permitted only after the operator has independently confirmed connectivity to the production Postgres target and has accepted the connectivity-path risk surface (TLS/SSL, rate limits, public exposure considerations). This runbook does NOT include literal commands, hostnames, ports, or URLs for either path; the operator chooses and invokes the approved execution context per their own Railway access procedures, with the connectivity preflight in §4(vii) confirming reachability before the runner is invoked.

## 4. Pre-flight checks (Codex Edit 1, applied verbatim)

> **Immediately before N-3 execution, verify all of the following in sequence; HALT on any failure:**
>
> (i) `git rev-parse HEAD` equals the exact commit hash named in Victor's explicit N-3 in-session production-action approval; that named hash must correspond to the latest committed runbook state (i.e., the HEAD that exists at approval time, after any N-2b/N-2c closeout commits). The runbook intentionally does not pin a hardcoded hash here, because committing a runbook that pins itself to its own pre-commit HEAD always produces a stale value the moment the commit lands. The approval-time exact-HEAD-naming rule (see §5) is the canonical source of the expected hash;
> (ii) `git branch --show-current` equals `main`;
> (iii) `git diff --name-status HEAD` is empty;
> (iv) `git status --short --untracked-files=all migrations/` is empty (no untracked migration files on disk);
> (v) `git ls-files migrations/` lists exactly `migrations/001_init.sql` through `migrations/008_emergency_audit_log.sql` and nothing else;
> (vi) operator verbally confirms the intended Railway service and production `DATABASE_URL` target without revealing the secret value.
> (vii) **Connectivity preflight (added in N-2d; tightened in N-2e).** From the chosen N-3 execution context (Railway-attached runtime/shell, or an explicitly accepted alternative connectivity path), the operator records PASS only after confirming, without pasting secrets or values, all four of the following outcomes: **(a) DNS / name resolution** succeeds from that context for the intended Postgres target; **(b) TLS / SSL negotiation** succeeds for the approved connection path, or is explicitly not applicable for the approved path; **(c) Postgres authentication** succeeds against the intended target (auth completes; no further query required); **(d) authenticated-target identity** is confirmed as the intended Railway project / service / database, by reading a non-secret service or add-on identity surface (e.g., the Railway dashboard view of the service / Postgres add-on the chosen context is attached to) or an approved read-only identity check that does not echo secret values. The operator records only PASS / FAIL category labels per (a)–(d); the operator must NOT record or paste the hostname, port, URL, username, password, `DATABASE_URL` value, or any query result value into chat or any handoff packet. If any of (a)–(d) fails, the operator classifies the failure as **DNS**, **TLS / SSL**, **auth**, or **wrong-target / unknown-target** and HALTs before the migration runner is invoked. Stale Railway CLI / project / service auth context is an instance of category (d) (wrong-target / unknown-target) and the operator must verify the CLI / project / service identity / context without exposing secrets as part of (d). The N-2c HALTED attempt (see §1.1) is the canonical category-(a) failure: a non-Railway-attached execution context produced a hostname-resolution failure (runner exit 1) before any SQL executed; this tightened preflight catches that class plus three additional classes proactively.
> (viii) **Concurrent-runner confirmation (added in N-2e).** The operator confirms that no other process, shell, deploy step, or Railway lifecycle hook is configured to invoke `scripts/run-migrations.js` (or any equivalent migration command) against the same target during the N-3 window. The runner is not mutex-guarded across processes; two concurrent invocations against the same `schema_migrations` would race. If a concurrent runner is configured (e.g., a Railway deploy step that auto-runs migrations on every deploy, an operator second shell, or a scheduled job), HALT until it is paused or the N-3 window is rescheduled.
> (ix) **No local `.env` shadow (added in N-2e).** The operator confirms that the execution context's working directory does NOT contain a `.env` file (or any other dotenv-loaded file the runner picks up at startup). The migration runner imports dotenv and reads `process.env.DATABASE_URL`; a stale or local `.env` file in the working directory could silently override the Railway-injected env and point the runner at a non-production target. If a `.env` file is present, HALT until it is removed, renamed outside the dotenv default load path, or the operator switches to a working directory that has no `.env`.
> (x) **Runtime-source identity confirmation (added in N-2f; tightened in N-2g).** The operator records PASS only after performing an **exact full-hash comparison** between the execution context's source identity and the **full HEAD hash named in Victor's fresh N-3 approval**. The comparison must use the full 40-character hash; near-HEAD identity, short-hash-only identity, branch-name-only identity (e.g., `main`), service-name-only identity (e.g., the Railway service name), and "latest deploy" identity are explicitly NOT sufficient. Procedure by execution-context type: **(a) Local execution** — run `git rev-parse HEAD` in the execution checkout that will invoke `scripts/run-migrations.js` and compare the returned 40-character hash byte-for-byte to the hash named in Victor's approval. **(b) Railway-attached execution** — read the deployed-revision / source-identity surface for the attached service (e.g., the Railway dashboard's deployed-commit hash for the service the runner will execute under, or an approved read-only identity check that does not echo secret values) and compare its full 40-character hash byte-for-byte to the hash named in Victor's approval; the surface chosen must NOT expose `DATABASE_URL`, hostnames, ports, or any other secret. **For both paths**, the operator additionally confirms that `scripts/run-migrations.js`, `migrations/008_emergency_audit_log.sql`, Node, and `node_modules` resolve from that exact-hash source / runtime; a deploy from a different commit, a stale `node_modules` from an earlier build, or a Node version drift produces a mismatch and HALT. If the full-hash comparison cannot be performed without exposing secrets, or if the comparison does not match exactly, HALT.
> (xi) **Pre-apply production DB baseline confirmation (added in N-2f; tightened in N-2g).** Immediately before invoking the migration runner — after the §4(viii) concurrent-runner confirmation has passed and with no other operations executed against the target between this check and runner invocation — the operator performs an approved read-only target-state check against the production target and records only PASS / FAIL labels (never query result values, never schema-row content, never secret values): **(a)** `schema_migrations` exists as a table in the target schema; **(b)** the exact query target `schema_migrations.version` returns exactly the integer set `{1, 2, 3, 4, 5, 6, 7}` — no missing version, no duplicate, no extra; **(c)** the value `8` is NOT present in `schema_migrations.version`; **(d)** no value greater than `8` is present in `schema_migrations.version`; **(e)** `emergency_audit_log` does not exist as a table or view in the target schema (verified via `information_schema.tables` / `information_schema.views` or an approved read-only equivalent). If any of (a)–(e) fails or cannot be verified from the chosen execution context, HALT before invoking the runner. **Timing constraint:** this baseline check must run last among §4 (i)–(xi), immediately preceding runner invocation; if the §4(viii) concurrent-runner confirmation cannot be re-asserted at the moment of (xi) (e.g., evidence emerges that another invocation may have started), HALT. **No-op runner output linkage:** if the runner is invoked after (xi) PASS but the captured runner output reports `all migrations applied` / no-op / does not contain an `applying 008_emergency_audit_log.sql` line, that observation is handled under §7 HALT-on-no-applying-008-line / HALT-on-all-migrations-applied — the operator does NOT re-run the runner; the operator HALTs and performs only the §6 (a)–(e) target-state verification before any further action. This baseline check guards against pre-existing malformed `emergency_audit_log` (which `CREATE TABLE IF NOT EXISTS` in the migration would no-op against), missing or mis-bootstrapped `schema_migrations`, and unexpected version drift in the target.
>
> **If any check (i)–(xi) differs from expected, HALT and re-request Codex review and operator approval before proceeding.**

**Note on the pre-existing untracked snapshot.** The known untracked file `position.json.snap.20260502T020154Z` lives at the repo root, **outside `migrations/`**. It is intentionally excluded from every prior commit. It does NOT cause check (iv) to fail, because that check is scoped to `migrations/` only. A non-empty `git status --short` (showing only that snap) at repo level is acceptable; a non-empty `git status --short --untracked-files=all migrations/` is NOT acceptable.

## 5. Operator-approval wording for N-3 (Codex Edit 2, applied verbatim)

> **Victor, approve Phase N-3 — Migration 008 production application only, for exact HEAD EXACT_CURRENT_HEAD_HASH_FROM_GIT_REV_PARSE_HEAD only.**
>
> **Scope:** files `migrations/008_emergency_audit_log.sql` and `scripts/run-migrations.js`; action: applying Migration 008 to the confirmed production Railway Postgres service and then running the listed read-only verification queries.
>
> **This approval does not authorize commits, deploys, Migration 009 or later, live exercises, Kraken actions, `MANUAL_LIVE_ARMED` reads/writes, env/secret changes, rollback, or any other production action.**
>
> **This approval expires when the single N-3 application-and-verification action ends.**

**Exact-HEAD-naming requirement (N-2c addition).** The N-3 approval must include the exact commit hash returned by `git rev-parse HEAD` immediately before the operator types the approval. The named hash binds the approval to a specific runbook state and is the canonical input for pre-flight check (i). If HEAD changes after the approval is given (a new commit lands, a rebase, a fast-forward pull, etc.) the approval expires and N-3 must HALT pending fresh Codex review and a fresh Victor approval that names the new HEAD. This avoids the self-staling commit loop that occurs when a runbook tries to pin itself to its own pre-commit HEAD.

The above is the **shape** of an approval. It is NOT itself an approval — Victor's actual in-session instruction (typed in the chat) is the approval. Per `orchestrator/APPROVAL-GATES.md` "What is NOT operator approval", a runbook field cannot substitute.

## 6. Verification queries — post-application (Codex Edit 3, applied verbatim)

> **After runner exit 0, verify in order:**
>
> **(a)** `SELECT version, name, checksum FROM schema_migrations WHERE version = 8` returns exactly one row with the expected values;
> **(b)** `SELECT version FROM schema_migrations ORDER BY version` returns exactly 1, 2, 3, 4, 5, 6, 7, 8 and no other rows;
> **(c)** `SELECT 1 FROM emergency_audit_log LIMIT 1` succeeds;
> **(d)** column definitions of `emergency_audit_log` match the migration exactly: column names, types, NOT NULL flags, defaults, primary key on `id`, `event_id` UNIQUE constraint, and `mode IN ('paper','live')` CHECK constraint (verify via `information_schema.columns`, `pg_constraint`, and `pg_index`/`pg_constraint` for PK / UNIQUE);
> **(e)** index definitions of `emergency_audit_log` match the migration exactly (verify via `pg_indexes` plus `pg_index`): six index names, the indexed columns and their sort order (including `DESC` where present in the migration), uniqueness / PK origin for the PK-derived and UNIQUE-derived indexes, and the partial predicates for the `unresolved` index (`WHERE resolved_at IS NULL`) and the `kraken_order_id` index (`WHERE kraken_order_id IS NOT NULL`).
>
> **Any extra migration version, missing object, missing index, or unexpected row content is a HALT.**

The six expected index names are:
1. `emergency_audit_log_pkey` (PK-derived, on `id`)
2. `emergency_audit_log_event_id_key` (UNIQUE-derived, on `event_id`)
3. `emergency_audit_log_unresolved_idx`
4. `emergency_audit_log_kraken_order_id_idx`
5. `emergency_audit_log_source_timestamp_idx`
6. `emergency_audit_log_failure_class_timestamp_idx`

The 13 expected column names (verification (d)) are: `id`, `event_id`, `timestamp`, `mode`, `source`, `kraken_order_id`, `failure_class`, `error_message`, `attempted_payload`, `resolved_at`, `resolved_by`, `resolution_notes`, `metadata`.

## 7. Emergency stop conditions during N-3 (Codex Edit 4, applied verbatim plus original list)

If any of these occur during N-3, **STOP immediately, surface the situation to the operator, and do not retry without re-review:**

**Original conditions (from N-2 plan):**
- Runner reports `CHECKSUM MISMATCH` (exit code 3) — an applied migration was edited; this is a security signal.
- Runner exits with code 4 (SQL failure) — surface the error message verbatim.
- Runner exits **1** for fatal connection / protocol errors (HALT — `DATABASE_URL` may be wrong target or unreachable; surface the runner's final error category from §4 (vii)). Runner exits **2** for missing `DATABASE_URL` or missing migrations directory (HALT — config or filesystem problem, not a DB connectivity failure; treat independently). **Do not conflate exit 1 and exit 2** — the recovery path differs (re-route execution context vs. fix env / filesystem).
- Verification query returns unexpected `schema_migrations` content.
- Verification query reports `emergency_audit_log` table does not exist.
- Any other tool or process attempting to write to `schema_migrations` or `emergency_audit_log` concurrently.

**Codex Edit 4 additions:**
> **HALT** if the runner output contains any `applying` log line other than `008_emergency_audit_log.sql`. **HALT** if the runner exits 0 but no `applying 008_emergency_audit_log.sql` log line appears in the captured runner output, because that contradicts the precondition that Migration 008 is unapplied. **HALT** on any `all migrations applied` / no-op output when N-3 expected to apply Migration 008 for the first time, unless the operator can verify, by completing the §6 (a)–(e) checks against the production target, that `schema_migrations` version 8 was inserted in the same invocation. **HALT (added in N-2f)** if the captured runner output is missing, incomplete, truncated, or cannot be reviewed well enough to classify the `applying` lines and final exit state. **HALT (added in N-2f)** if a connection or protocol failure occurs after an `applying 008_emergency_audit_log.sql` line appears or during / after `COMMIT`; classify the database outcome as **unknown** (not "succeeded" and not "rolled back"); do not retry; do not rollback; do not invoke `DROP TABLE` or any compensating DDL; perform only the approved read-only §6 (a)–(e) target-state verification before any further action. The committed-or-not outcome is determined by §6 verification alone; until §6 confirms version 8 inserted with the expected schema, the database state is treated as indeterminate. **HALT** if any verification query returns a result inconsistent with expected values. **HALT** if there is any evidence of a concurrent migration runner or concurrent writer to `schema_migrations` or `emergency_audit_log`.

**N-2d additions (connectivity-related):**
> **HALT** if the chosen N-3 execution context cannot resolve or reach the production Postgres target before the runner is invoked (per pre-flight §4(vii)). Do not invoke the runner. Do not retry from the same un-validated execution context. Do not loop on retry attempts. Surface the unreachable-target observation to the operator and await fresh operator instruction.
> **HALT** if the runner exits with a connection failure (exit 1 with hostname-resolution / connection-refused / TLS-handshake errors, or exit 2 for missing `DATABASE_URL` / missing migrations directory) — the prior Victor approval is consumed by the failed attempt and any retry requires a fresh Codex N-3 preflight PASS plus a fresh Victor in-session production-action approval naming the exact current HEAD. Do not retry automatically.
> **Do not retry automatically under any circumstance.** Every retry is a fresh execution attempt requiring its own fresh Codex review and fresh Victor approval. The runbook §11 N-3 gate must be re-satisfied from scratch on every retry.

## 8. Risk table (Codex Edit 5 additions integrated)

| Tier | Risk | Mitigation |
|---|---|---|
| **P0** | Runner applies an unintended migration (e.g., a tracked 009+ that lands later) | Pre-flight (v): `git ls-files migrations/` shows exactly 001..008. |
| **P0** | **Untracked or dirty `migrations/009+` file on disk at execution time** (filesystem-level, not git-level — runner reads filesystem at lines 49–51) | **Pre-flight (iv): `git status --short --untracked-files=all migrations/` is empty.** *(Codex Edit 5 addition.)* |
| **P0** | **Operator running from a non-`main` branch or wrong HEAD** where additional migrations exist, OR HEAD changes after Victor's approval (new commit, rebase, fast-forward pull) and approval becomes stale | **Pre-flight (i, ii): `git rev-parse HEAD` equals the exact hash named in Victor's N-3 approval (per §5 exact-HEAD-naming requirement); `git branch --show-current` equals `main`. If HEAD changes after approval, HALT pending fresh Codex review and fresh Victor approval naming the new HEAD.** *(Codex Edit 5 addition; reformulated in N-2c to avoid self-staling commit loop.)* |
| **P0** | Wrong production DB target (Railway points at staging, or `DATABASE_URL` is dev/staging) | Pre-flight (vi): operator verbally confirms target service / DB without revealing secret value. **Pre-flight (vii) category (d): authenticated-target identity confirmed as the intended Railway project / service / database via a non-secret service / add-on identity surface or approved read-only identity check.** Both checks must PASS independently; either alone is insufficient. *(Mitigation chain extended in N-2g to also cite §4(vii)(d).)* |
| **P0** | **Network partition or connection loss during / after `COMMIT` leaves the operator unable to know whether Migration 008 committed** *(reframed in N-2f)* | Per-migration `BEGIN`/`COMMIT`/`ROLLBACK` prevents half-committed DDL, but the client-observed outcome can be unknown after a COMMIT-path failure. Treat as HALT; do not retry; do not rollback; do not invoke `DROP TABLE` or compensating DDL; perform only approved read-only §6 (a)–(e) target-state verification before any further action. The committed-or-not outcome is determined by §6 verification alone. |
| **P0** | Concurrent migration runner invocation | Pre-flight (viii): operator confirms no concurrent invocation. **The runner does not take an advisory lock and concurrent invocations can race on `schema_migrations`** — the runner reads `schema_migrations` (`scripts/run-migrations.js:87–91`) before writing, and two concurrent processes can both observe the same pre-state. Prevention is procedural: if concurrent invocation cannot be ruled out, HALT before invoking the runner. *(Corrected in N-2f — prior wording incorrectly claimed Postgres row-level locks would serialize; that is not what the runner does.)* |
| **P1** | **Operator treats Codex PASS, clean working tree, or successful pre-flight as production-action approval** (forbidden per `APPROVAL-GATES.md:102–112`) | **Explicit policy citation in §9 Safety Gates; approval-wording gate in §5.** *(Codex Edit 5 addition.)* |
| **P1** | **Operator-approval wording inadvertently containing real secrets, env values, `DATABASE_URL` contents, or live order details** (forbidden per `HANDOFF-RULES.md:59–67`) | **Approval wording must be reviewed before use; pre-flight (vi) explicitly says "without revealing the secret value".** *(Codex Edit 5 addition.)* |
| **P1** | Future `emergency_audit_log` writes contain a real secret (helper bug) | Runtime defense: `_redactAttemptedPayload` (committed D-5.12c, Codex-reviewed). Migration itself does not introduce risk. |
| **P1** | Operator confuses N-3 approval with broader live-exercise approval | One-time-scope wording in §5 explicitly excludes deploys, live exercises, `MANUAL_LIVE_ARMED`, env changes, rollback. |
| **P1** | `emergency_audit_log` payload growth without retention | Tracked in Codex v4 LOW note 9.b; deferred to D-5.12i operator playbook. Not a current risk; expected zero rows pre-incident. |
| **P1** | Railway log retention is finite | Operator captures runner stdout/stderr to a local timestamped file in N-3 (see §10). |
| **P0** | **Local execution context cannot resolve or reach the Railway-internal Postgres hostname** (e.g., a `getaddrinfo ENOTFOUND` for the internal hostname when running outside Railway's network) — runner exits 1 before any SQL executes (no transaction, no partial state, no rollback needed) | **Pre-flight (vii) connectivity preflight: confirm reachability from the chosen execution context before invoking the runner. Preferred path: a Railway-attached runtime / shell where the internal hostname resolves. Alternative paths must be operator-confirmed for connectivity AND must accept the corresponding risk surface.** *(N-2d addition; observed during the N-3 attempt on 2026-05-03 — see §1.1.)* |
| **P1** | **Operator retries N-3 reusing a consumed approval** (the prior Victor approval was bound to a single application-and-verification action; once that action ends — successfully or via HALT — the approval is consumed) | **Each retry requires a fresh Codex N-3 preflight PASS on the current runbook plus a fresh Victor in-session production-action approval naming the exact current HEAD at retry time. Per §5 exact-HEAD-naming requirement and §11 N-3 gate, the gate is re-satisfied from scratch on every retry.** *(N-2d addition.)* |
| **P1** | **Concurrent migration runner from a second shell, deploy step, or Railway lifecycle hook** invokes `scripts/run-migrations.js` (or any equivalent migration command) against the same target during the N-3 window. The runner is not mutex-guarded across processes; both instances would read `schema_migrations` before either writes; one or both could attempt identical INSERTs or produce race-condition errors against `schema_migrations` / `emergency_audit_log`. | **Pre-flight (viii): operator confirms no other process, shell, deploy step, or Railway lifecycle hook is configured to invoke the migration runner against the same target during the N-3 window. If a concurrent runner is configured, HALT until it is paused or the N-3 window is rescheduled.** *(N-2e addition.)* |
| **P1** | **`DATABASE_URL` or other env var shadowed by a local `.env` file** in the execution context's working directory. The migration runner imports dotenv at startup and reads `process.env.DATABASE_URL`; a stale or local `.env` file could silently override the Railway-injected env and point the runner at a non-production target. | **Pre-flight (ix): operator confirms no `.env` file is present in the execution context's working directory. If a `.env` file is present, HALT until it is removed, renamed outside the dotenv default load path, or the operator switches to a working directory that has no `.env`.** *(N-2e addition.)* |
| **P1** | **Stale Railway CLI / project / service auth context** — `railway run` / `railway shell` / equivalent execution surface may be scoped to a different Railway project, service, or environment than intended, causing the runner to authenticate against a non-production target without surfacing it as an error. | **Pre-flight (vii) category (d) "wrong-target / unknown-target": operator verifies the Railway CLI / project / service identity and context without exposing secrets, by reading a non-secret service or add-on identity surface (e.g., the Railway dashboard view of the attached service / Postgres add-on) before invoking the runner. If the identity / context cannot be verified or does not match the intended Railway project / service / database, HALT.** *(N-2e addition.)* |
| **P1** | **Runner version drift between approved HEAD and Railway-attached execution** — the `package.json` / `node_modules` / Node version in the Railway-attached execution context may differ from the local approved HEAD, causing the runner file at the approved HEAD to behave differently at runtime; or a near-HEAD / short-hash / branch-name / "latest deploy" identity could be mistaken for the approved HEAD. | **Pre-flight (x) full-hash comparison: operator records PASS only after performing an exact byte-for-byte comparison of the execution context's source-identity full 40-character hash against the full HEAD hash named in Victor's fresh N-3 approval. For local execution, `git rev-parse HEAD` from the execution checkout. For Railway-attached execution, the deployed-revision / source-identity surface for the attached service. Near-HEAD / short-hash / branch-name / service-name / "latest deploy" identity is explicitly NOT sufficient.** *(N-2e addition; promoted P2 → P1 and tied to explicit pre-flight (x) in N-2f; mitigation tightened in N-2g to require exact full-hash comparison.)* |
| **P1** | **Pre-existing `emergency_audit_log` table or unexpected `schema_migrations` baseline state in production** — `CREATE TABLE IF NOT EXISTS` in the migration would no-op against a pre-existing malformed table; the runner would then record version 8 as applied without actually applying the 008 DDL, leaving the production schema silently wrong. Or `schema_migrations` could be missing, mis-bootstrapped, or contain a version > 8. | **Pre-flight (xi): operator performs read-only baseline confirmation immediately before runner invocation (after the §4(viii) concurrent-runner confirmation) — `schema_migrations` exists; the exact query target `schema_migrations.version` returns exactly the integer set `{1, 2, 3, 4, 5, 6, 7}`; version 8 is absent; no value > 8 is present; `emergency_audit_log` does not exist as a table or view. PASS / FAIL labels only; never query result values. If any baseline condition fails or cannot be verified, HALT before invoking the runner. No-op / all-migrations-applied runner output after (xi) PASS is handled under §7 HALT-on-no-applying-008-line / HALT-on-all-migrations-applied — do NOT re-run the runner.** *(N-2f addition; mitigation tightened in N-2g to name `schema_migrations.version` query target, require immediate-before-invocation timing after §4(viii), and link no-op runner output to §7 HALT.)* |
| **P2** | Index creation contention | NONE — empty table; index creation is instantaneous. |
| **P2** | Future schema migration depends on `emergency_audit_log` existing | Bounded; would be an explicit dependency in a future migration. |
| **P2** | Application of 008 negatively affects `bot.js` / `dashboard.js` runtime | LOW. D-5.12c/d/e helpers write to this table only on actual DB-failure paths (steady-state: zero rows expected pre-incident). |

## 9. Safety gates and approvals

Per `orchestrator/APPROVAL-GATES.md`:

| Action | Gate | Status |
|---|---|---|
| Migration 008 file commit (D-5.12c, `4ae3689`) | Gate 3 | **Already given** (Codex round-2 PASS + operator approval; scoped to D-5.12c) |
| **N-3: apply Migration 008 to production** | **Gate 4** (production migration application — separate from gate 3) | **Required, not yet granted** |
| Production Railway runner invocation against production target | Gate 5 (Railway deployment / production target) | **Required, not yet granted** |
| Production-DB schema mutation | Gate 12 (production-state mutation) | **Required, not yet granted** |

**Gate-4 separation rule.** Per `APPROVAL-GATES.md` "Production actions require separate explicit approval from code/doc commits": (a) any prior commit-time approval (D-5.12c, ARC-7) does NOT authorize gate 4; (b) any N-2 / N-2b / N-2c / N-2d / N-2e / N-2f commit-time approval will NOT authorize gate 4; (c) any Codex PASS — including a Codex PASS on this runbook — does NOT authorize gate 4. Per `AUTOMATION-PERMISSIONS.md` RED tier and `NEXT-ACTION-SELECTOR.md` rule 6, automation cannot grant gate 4. **Only Victor's explicit, in-session, scoped instruction authorizes N-3.**

## 10. Runner output capture (recommended for N-3)

Capture full runner stdout and stderr to a local timestamped file during N-3 execution. Railway log retention is finite (Codex v4 LOW note 9.d), and the runner output contains the per-migration `applying` log lines that the operator must verify against the §7 stop conditions ("HALT if the runner output contains any `applying` log line other than `008_emergency_audit_log.sql`").

Suggested form (operator-driven; not authorized by this runbook): during N-3, the operator should use the explicitly approved production runner invocation from Victor's in-session approval and redirect stdout and stderr to a local timestamped file outside the repository. This runbook intentionally omits the literal command because `HANDOFF-RULES.md` forbids production migration-apply commands inside packet artifacts.

The capture file is local to the operator's machine and outside the repository. It is read-only after capture; do not paste its contents into any handoff packet (`HANDOFF-RULES.md` forbids prod-DB content / log content in packets).

## 11. Final state — N-3 blocking conditions

**N-3 must remain blocked until ALL of the following are satisfied, in order:**

1. **Codex returns PASS on this N-2g docs-only runbook update.** Binding state is the latest PASS verdict on the N-2g artifact in `orchestrator/handoffs/CODEX-VERDICT.md`.
2. **Codex returns a fresh N-3 preflight PASS on the updated runbook before operator approval is requested.** Any prior N-3 preflight PASS issued against an earlier runbook revision is consumed / stale and cannot stand in for this fresh review.
3. **Victor / CEO gives explicit, in-session, scoped production-action approval** matching the §5 approval wording substance and naming the exact current HEAD hash (per the §5 exact-HEAD-naming requirement). The approval is the operator's typed in-session instruction, not a runbook field; it is bound to the named HEAD and expires if HEAD changes before execution.
4. **All pre-flight checks (§4 (i)–(xi)) pass** at the moment of N-3 execution, including the (vii) connectivity preflight (DNS / TLS / auth / target-identity categories), (viii) concurrent-runner confirmation, (ix) no-local-`.env`-shadow confirmation, (x) runtime-source identity confirmation, and (xi) pre-apply production DB baseline confirmation. If any check fails, HALT.
5. **Operator confirms the target Railway service and production `DATABASE_URL`** without exposing secret values.

Until all five conditions are satisfied simultaneously, **no migration runner is invoked against production, no production Railway runner command is executed, and no direct database-URL runner invocation is executed.** Per `AUTOMATION-PERMISSIONS.md` RED tier and `HANDOFF-RULES.md` forbidden-content rule, no automation may treat this runbook's existence, completeness, Codex verdict, or pre-flight passing as approval.

## 12. Cross-references

- `migrations/008_emergency_audit_log.sql` — the migration under review.
- `scripts/run-migrations.js` — the runner that would apply it (D-5.4).
- `orchestrator/HANDOFF-RULES.md` — packet rules (this runbook is a packet artifact).
- `orchestrator/APPROVAL-GATES.md` — 16-gate matrix and gate-4 separation rule.
- `orchestrator/PROTECTED-FILES.md` — per-path classification (Migration 008 is HARD BLOCK; this runbook is SAFE).
- `orchestrator/PHASE-MODES.md` — Mode 6 PRODUCTION ACTION precondition (plan/checklist/runbook before operator approval).
- `orchestrator/NEXT-ACTION-SELECTOR.md` — master order; canonical authority over packets.
- `orchestrator/ROLE-HIERARCHY.md` — Codex reviewer role (PASS necessary, not sufficient); Victor sole final approver.
- `orchestrator/AUTOMATION-PERMISSIONS.md` — RED tier for production migration application; blocked-commands list.
- `orchestrator/AUTOPILOT-RULES.md` — supervised-autopilot rules.
- `orchestrator/BLUEPRINT.md` — architectural blueprint and Safety Enforcement Layer.
- `CLAUDE.md` (repo root) — top-level role / safety / change-discipline rules.

## 13. What this runbook is NOT

- Not an approval.
- Not a commit.
- Not authorization for any production action.
- Not canonical over `orchestrator/NEXT-ACTION.md` / `orchestrator/NEXT-ACTION-SELECTOR.md`.
- Not a substitute for Codex review or for Victor's in-session approval.
- Not a script to be executed automatically. Every command shown is documentation; only Victor's explicit instruction at N-3 authorizes any execution.

## 14. Change history

- **N-2b (2026-05-03):** Initial runbook artifact derived from the N-2 conversation-only design report. Codex's five required tightening edits applied verbatim: (1) pre-flight expanded with branch / HEAD / untracked-files / dirty-tree checks; (2) operator-approval wording stabilized; (3) verification expanded to six indexes + full `schema_migrations` sequence + 13-column check; (4) emergency stop conditions extended with applying-log-line check; (5) risk table extended with two new P0 risks (untracked/dirty 009+; wrong branch/HEAD) and two new P1 risks (mistaking Codex PASS / clean tree as approval; approval wording leaking secrets). Three follow-up rounds of cleanup applied to remove literal production runner commands and reach full prose-only HANDOFF-RULES.md compliance. Committed at `e6c9189`.
- **N-2c (2026-05-03):** HEAD-preflight correction. Codex N-3 preflight review caught that pre-flight check (i) at line 47 still pinned HEAD to `8266db2…` (the pre-N-2b commit), while the runbook is now committed at `e6c9189`. Naively updating the pinned hash to `e6c9189…` would produce the same staleness the moment N-2c itself commits — a self-staling commit loop. N-2c instead replaces the in-runbook hardcoded pin with an approval-time exact-HEAD-naming rule: pre-flight check (i) now requires HEAD to equal the exact hash named in Victor's N-3 approval; §5 adds an "exact-HEAD-naming requirement" that binds Victor's approval to a specific HEAD and expires the approval if HEAD changes; the risk table is reformulated for the same. Committed at `9ae139d`.
- **N-3 attempt (2026-05-03):** Single attempt, HALTED before any SQL executed. Runner exit 1; final log line reported `getaddrinfo ENOTFOUND` for the Railway-internal Postgres hostname. No `applying` log line appeared. No `BEGIN` issued. No SQL executed. Production-DB state unchanged. Migration 008 still NOT applied. No partial state. No rollback needed. Prior Victor approval (which named exact HEAD `9ae139d…`) was consumed by the failed attempt and cannot be reused. Captured runner output preserved as a local timestamped file on the operator's machine. See §1.1 N-3 attempt history for the canonical record.
- **N-2d (2026-05-03):** Connectivity-preflight runbook update. Adds §1.1 N-3 attempt history; extends §3 with a Railway-attached execution-context requirement; adds pre-flight check (vii) connectivity preflight requiring the operator to confirm reachability of the production Postgres target from the chosen execution context before the runner is invoked; extends §7 emergency stop conditions with HALT-on-unreachable-target and HALT-on-connection-failure rules and a do-not-retry-automatically reminder; adds two new risk-table rows (P0 for local execution context cannot resolve Railway-internal hostname; P1 for retrying with a consumed approval). The runbook remains fully prose-only and HANDOFF-RULES.md compliant — no literal Railway runner commands, no `DATABASE_URL` examples, no hostname / port / URL values added. **Committed at HEAD `3732721`. Descriptive history only. This entry does not authorize N-3; N-3 remains blocked behind §11.**
- **N-2e (2026-05-03):** Codex-required N-3 preflight tightening. The Codex N-3 preflight review on the N-2d-committed runbook returned PASS WITH REQUIRED EDITS over eight surfaces. N-2e applies all eight edits verbatim and adds two new pre-flight checks: (1) §4(vii) connectivity preflight tightened from prose-only "reachable" to four PASS/FAIL categories — DNS / TLS-SSL / Postgres auth / authenticated-target identity — recorded as category labels only, never with hostname / port / URL / username / password / `DATABASE_URL` value / query result; (2) §5 approval-wording block extended with an explicit `for exact HEAD EXACT_CURRENT_HEAD_HASH_FROM_GIT_REV_PARSE_HEAD only` placeholder so the highest-salience approval text mirrors the §5 exact-HEAD-naming requirement; (3) §6 verification expanded from column-name + index-name checks to full column-definition checks (names, types, NOT NULL flags, defaults, PK, `event_id` UNIQUE, `mode` CHECK) and full index-definition checks (six index names, indexed columns, sort order including DESC where present, uniqueness / PK origin, partial predicates for the unresolved and `kraken_order_id` indexes); (4) §7 exit-code stop condition disambiguated — exit 1 = fatal connection / protocol; exit 2 = missing `DATABASE_URL` / missing migrations directory; do not conflate; (5) §7 unexpected runner-output stop condition extended with HALT-on-no-applying-008-line on exit 0, and HALT-on-all-migrations-applied output unless §6 (a)–(e) verifies same-invocation application; (6) §8 risk table extended with four new rows (P1 concurrent runner from a second shell / deploy step / Railway lifecycle hook; P1 `DATABASE_URL` / env shadowed by local `.env`; P1 stale Railway CLI / project / service auth context; P2 runner version drift between approved HEAD and Railway-attached execution); (7) §9 gate-separation note updated from "any future N-2b commit-time approval" to "any N-2 / N-2b / N-2c / N-2d / N-2e commit-time approval"; (8) §14 N-2d entry stale-state correction from "Pending Codex docs-only review and explicit operator approval before commit" to "Committed at HEAD `3732721` … N-3 remains blocked behind §11". Pre-flight expands from seven checks to nine: (viii) concurrent-runner confirmation; (ix) no local `.env` shadow in execution-context working directory. The §11 N-3 gate condition (4) updated to require all nine pre-flight checks (i)–(ix) PASS; STATUS.md / CHECKLIST.md / NEXT-ACTION.md updated to mirror the seven→nine derivative reference. The runbook remains fully prose-only and HANDOFF-RULES.md compliant — no literal Railway runner commands, no `DATABASE_URL` values, no hostname / port / URL values added. **Committed at HEAD `afe94d1`. Descriptive history only. This entry does not authorize N-3; N-3 remains blocked behind §11.**
- **N-2f (2026-05-03):** Codex-required N-3 preflight DDL-baseline + rollback-blocker tightening. The Codex N-3 preflight review on the N-2e-committed runbook (HEAD `afe94d1`) returned PASS WITH REQUIRED EDITS over eight surfaces. N-2f applies all eight edits verbatim and adds two new pre-flight checks: (1) §2 explicit forward-only rollback blocker prohibiting rollback / `DROP TABLE` / compensating DDL / any production schema mutation after a failed N-3 attempt without a separate Codex-reviewed rollback runbook + fresh Victor approval; (2) §4(x) runtime-source identity confirmation — operator confirms execution context resolves runner / migration / Node / `node_modules` from the approved HEAD / runtime; (3) §4(xi) pre-apply production DB baseline confirmation — `schema_migrations` exists with exactly versions 1–7, version 8 absent, no version > 8, `emergency_audit_log` does not exist, PASS / FAIL labels only; (4) §4 closing HALT sentence updated from `(i)–(ix)` to `(i)–(xi)`; (5) §7 HALT rules extended with HALT-on-missing-or-truncated-capture and HALT-on-COMMIT-path-uncertainty (database outcome unknown; do not retry; do not rollback; perform only read-only §6 verification); (6) §8 risk table corrected — P0 network-partition row reframed to outcome-unknown semantics, P0 concurrent-runner row corrected to remove the false advisory-lock claim and clarify procedural-only prevention, P2 runner-version-drift row promoted to P1 and tied to new pre-flight (x), new P1 row added for pre-existing `emergency_audit_log` / unexpected `schema_migrations` baseline tied to new pre-flight (xi); (7) §11 N-3 gate condition (1) updated from "N-2d" to "N-2f", condition (4) updated from `(i)–(ix)` to `(i)–(xi)` listing the two new checks; (8) §14 N-2e entry stale-state correction from "Pending Codex docs-only review and explicit operator approval before commit" to "Committed at HEAD `afe94d1`. Descriptive history only.". Pre-flight expands from nine checks to eleven. STATUS.md / CHECKLIST.md / NEXT-ACTION.md updated to mirror the nine → eleven derivative reference. The runbook remains fully prose-only and HANDOFF-RULES.md compliant — no literal Railway runner commands, no `DATABASE_URL` values, no hostname / port / URL values added. **Committed at HEAD `3af1e44daf736b825f9f352a66304bce28755e85`. Descriptive history only. This entry does not authorize N-3; N-3 remains blocked behind §11.**
- **N-2g (2026-05-03):** Codex-required N-3 preflight runtime-identity + DB-baseline + descriptive-state tightening. The Codex N-3 preflight review on the N-2f-committed runbook (HEAD `3af1e44`) returned FAIL with four required edits: (a) §4(x) under-specified — the runtime-source identity check gave only permissive examples and would let a near-HEAD / short-hash / branch-name / service-name / "latest-deploy" identity pass; (b) §4(xi) under-specified — the pre-apply DB baseline check did not name the exact query target, did not constrain timing relative to the §4(viii) concurrent-runner confirmation, and did not link runner no-op output to §7; (c) §8 mitigation mappings inherited the under-specified (x) and (xi) chains, plus the wrong-target row cited only §4(vi) and not §4(vii)(d); (d) §14 / phase label / status chain stale — the runbook header still read "Phase: N-2b", the §1 status chain stopped at `afe94d1`, and the §14 N-2f tail still said "Pending Codex docs-only review and explicit operator approval before commit" even though N-2f committed at `3af1e44`. N-2g applies all four required edits: (1) tightens §4(x) to require an exact full-hash comparison between the execution context's source identity and the full HEAD hash named in Victor's fresh N-3 approval — for local execution, compare `git rev-parse HEAD` from the execution checkout; for Railway-attached execution, use a non-secret deployed-revision / source-identity surface that exposes no secrets and confirm the full deployed / source revision hash equals Victor's approved full hash — and explicitly excludes near-HEAD / short-hash / branch-name / service-name / "latest deploy" identity; (2) tightens §4(xi) to name the exact query target (`schema_migrations.version`), require the baseline check to immediately precede runner invocation after the §4(viii) concurrent-runner confirmation, and explicitly link no-op / all-migrations-applied runner output to §7 HALT; (3) updates §8 risk-table mitigation chains — runner-version-drift cites the tightened §4(x) full-hash comparison, pre-existing-baseline-drift cites the tightened §4(xi) `schema_migrations.version` query and timing, wrong-target cites both §4(vi) and §4(vii)(d); (4) updates the runbook header phase label from "N-2b" to "N-2g", extends the §1 status chain to include `3af1e44` (N-2f), and corrects the §14 N-2f entry tail. Pre-flight remains at eleven checks (i)–(xi); no new check added in N-2g. The runbook remains fully prose-only and HANDOFF-RULES.md compliant — no literal Railway runner commands, no `DATABASE_URL` values, no hostname / port / URL values added. N-3 remains blocked pending Codex docs-only PASS on this N-2g update, fresh Codex N-3 preflight PASS on the updated runbook, fresh Victor explicit in-session production-action approval naming the exact current HEAD, all eleven pre-flight checks (i)–(xi) passing at execution time, and target confirmation without exposing secrets. Pending Codex docs-only review and explicit operator approval before commit.
